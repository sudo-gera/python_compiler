#!/usr/bin/env python3.8
# @generated by pegen from python.gram

import ast
import sys
import tokenize

from typing import Any, Optional

from pegen.parser import memoize, memoize_left_rec, logger, Parser
# Keywords and soft keywords are listed at the end of the parser definition.
class GeneratedParser(Parser):

    @memoize
    def spaces(self) -> Optional[Any]:
        # spaces: spaces_in_brackets | spaces_not_in_brackets
        mark = self._mark()
        if (
            (spaces_in_brackets := self.spaces_in_brackets())
        ):
            return spaces_in_brackets;
        self._reset(mark)
        if (
            (spaces_not_in_brackets := self.spaces_not_in_brackets())
        ):
            return spaces_not_in_brackets;
        self._reset(mark)
        return None;

    @memoize
    def spaces_in_brackets(self) -> Optional[Any]:
        # spaces_in_brackets: r'([^\S\n]|\\\n|\n)*'
        mark = self._mark()
        if (
            (token := self.expect(r'([^\S\n]|\\\n|\n)*'))
        ):
            return token if self . _bracket_level else None;
        self._reset(mark)
        return None;

    @memoize
    def spaces_not_in_brackets(self) -> Optional[Any]:
        # spaces_not_in_brackets: r'([^\S\n]|\\\n)*'
        mark = self._mark()
        if (
            (token := self.expect(r'([^\S\n]|\\\n)*'))
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def spaces_for_indent(self) -> Optional[Any]:
        # spaces_for_indent: r'[^\S\n]*'
        mark = self._mark()
        if (
            (token := self.expect(r'[^\S\n]*'))
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def new_line(self) -> Optional[Any]:
        # new_line: r'(#[^\n]*)?(\n|$)'
        mark = self._mark()
        if (
            (token := self.expect(r'(#[^\n]*)?(\n|$)'))
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def m_token(self) -> Optional[Any]:
        # m_token: r'\*|@|//|/|%' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\*|@|//|/|%'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def a_token(self) -> Optional[Any]:
        # a_token: r'\+|-' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\+|-'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def shift_token(self) -> Optional[Any]:
        # shift_token: r'<<|>>' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'<<|>>'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def bit_and_token(self) -> Optional[Any]:
        # bit_and_token: r'&' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'&'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def bit_xor_token(self) -> Optional[Any]:
        # bit_xor_token: r'\^' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\^'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def bit_or_token(self) -> Optional[Any]:
        # bit_or_token: r'\|' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\|'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def comp_token(self) -> Optional[Any]:
        # comp_token: r'<|>|==|>=|<=|!=' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'<|>|==|>=|<=|!='))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def aug_token(self) -> Optional[Any]:
        # aug_token: r'\+=|-=|\*=|@=|//=|/=|%=|\*\*=|>>=|<<=|&=|\^=|\|=' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\+=|-=|\*=|@=|//=|/=|%=|\*\*=|>>=|<<=|&=|\^=|\|='))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def u_token(self) -> Optional[Any]:
        # u_token: r'-|\+|~' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'-|\+|~'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def colon_token(self) -> Optional[Any]:
        # colon_token: r':' spaces
        mark = self._mark()
        if (
            (token := self.expect(r':'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def walrus_token(self) -> Optional[Any]:
        # walrus_token: r':=' spaces
        mark = self._mark()
        if (
            (token := self.expect(r':='))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def semicolon_token(self) -> Optional[Any]:
        # semicolon_token: r';' spaces
        mark = self._mark()
        if (
            (token := self.expect(r';'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def dot_token(self) -> Optional[Any]:
        # dot_token: r'\.' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\.'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def comma_token(self) -> Optional[Any]:
        # comma_token: r',' spaces
        mark = self._mark()
        if (
            (token := self.expect(r','))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def star_token(self) -> Optional[Any]:
        # star_token: r'\*' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\*'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def starstar_token(self) -> Optional[Any]:
        # starstar_token: r'\*\*' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\*\*'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def assign_token(self) -> Optional[Any]:
        # assign_token: r'=' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'='))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def returns_token(self) -> Optional[Any]:
        # returns_token: r'->' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'->'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def decorator_token(self) -> Optional[Any]:
        # decorator_token: r'@' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'@'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def slash_token(self) -> Optional[Any]:
        # slash_token: r'/' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'/'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def left_paren_token(self) -> Optional[Any]:
        # left_paren_token: r'\(' start_bracket spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\('))
            and
            (self.start_bracket())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def right_paren_token(self) -> Optional[Any]:
        # right_paren_token: r'\)' stop_bracket spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\)'))
            and
            (self.stop_bracket())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def left_bracket_token(self) -> Optional[Any]:
        # left_bracket_token: r'\[' start_bracket spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\['))
            and
            (self.start_bracket())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def right_bracket_token(self) -> Optional[Any]:
        # right_bracket_token: r'\]' stop_bracket spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\]'))
            and
            (self.stop_bracket())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def left_brace_token(self) -> Optional[Any]:
        # left_brace_token: r'\{' start_bracket spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\{'))
            and
            (self.start_bracket())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def right_brace_token(self) -> Optional[Any]:
        # right_brace_token: r'\}' stop_bracket spaces
        mark = self._mark()
        if (
            (token := self.expect(r'\}'))
            and
            (self.stop_bracket())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def if_token(self) -> Optional[Any]:
        # if_token: r'if\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'if\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def elif_token(self) -> Optional[Any]:
        # elif_token: r'elif\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'elif\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def else_token(self) -> Optional[Any]:
        # else_token: r'else\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'else\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def for_token(self) -> Optional[Any]:
        # for_token: r'for\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'for\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def in_token(self) -> Optional[Any]:
        # in_token: r'in\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'in\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def is_token(self) -> Optional[Any]:
        # is_token: r'is\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'is\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def not_token(self) -> Optional[Any]:
        # not_token: r'not\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'not\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def while_token(self) -> Optional[Any]:
        # while_token: r'while\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'while\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def async_token(self) -> Optional[Any]:
        # async_token: r'async\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'async\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def await_token(self) -> Optional[Any]:
        # await_token: r'await\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'await\b'))
            and
            (self.spaces())
        ):
            return token if self . _func_level [- 1] == 2 else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def return_token(self) -> Optional[Any]:
        # return_token: r'return\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'return\b'))
            and
            (self.spaces())
        ):
            return token if self . _func_level [- 1] else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def yield_token(self) -> Optional[Any]:
        # yield_token: r'yield\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'yield\b'))
            and
            (self.spaces())
        ):
            return token if self . _func_level [- 1] else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def from_token(self) -> Optional[Any]:
        # from_token: r'from\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'from\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def lambda_token(self) -> Optional[Any]:
        # lambda_token: r'lambda\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'lambda\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def pass_token(self) -> Optional[Any]:
        # pass_token: r'pass\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'pass\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def del_token(self) -> Optional[Any]:
        # del_token: r'del\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'del\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def raise_token(self) -> Optional[Any]:
        # raise_token: r'raise\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'raise\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def assert_token(self) -> Optional[Any]:
        # assert_token: r'assert\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'assert\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def break_token(self) -> Optional[Any]:
        # break_token: r'break\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'break\b'))
            and
            (self.spaces())
        ):
            return token if self . _loop_level [- 1] else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def continue_token(self) -> Optional[Any]:
        # continue_token: r'continue\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'continue\b'))
            and
            (self.spaces())
        ):
            return token if self . _loop_level [- 1] else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def import_token(self) -> Optional[Any]:
        # import_token: r'import\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'import\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def as_token(self) -> Optional[Any]:
        # as_token: r'as\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'as\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def global_token(self) -> Optional[Any]:
        # global_token: r'global\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'global\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def nonlocal_token(self) -> Optional[Any]:
        # nonlocal_token: r'nonlocal\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'nonlocal\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def type_token(self) -> Optional[Any]:
        # type_token: r'type\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'type\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def and_token(self) -> Optional[Any]:
        # and_token: r'and\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'and\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def or_token(self) -> Optional[Any]:
        # or_token: r'or\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'or\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def def_token(self) -> Optional[Any]:
        # def_token: r'def\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'def\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def try_token(self) -> Optional[Any]:
        # try_token: r'try\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'try\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def except_token(self) -> Optional[Any]:
        # except_token: r'except\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'except\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def finally_token(self) -> Optional[Any]:
        # finally_token: r'finally\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'finally\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def with_token(self) -> Optional[Any]:
        # with_token: r'with\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'with\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def match_token(self) -> Optional[Any]:
        # match_token: r'match\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'match\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def case_token(self) -> Optional[Any]:
        # case_token: r'case\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'case\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def class_token(self) -> Optional[Any]:
        # class_token: r'class\b' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'class\b'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def start_stre_q(self) -> Optional[Any]:
        # start_stre_q: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _stre_level . append ( '\042' )];
        self._reset(mark)
        return None;

    @memoize
    def start_stre_a(self) -> Optional[Any]:
        # start_stre_a: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _stre_level . append ( '\047' )];
        self._reset(mark)
        return None;

    @memoize
    def start_stre_b(self) -> Optional[Any]:
        # start_stre_b: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _stre_level . append ( '\175' )];
        self._reset(mark)
        return None;

    @memoize
    def stre(self) -> Optional[Any]:
        # stre: r'.'
        mark = self._mark()
        if (
            (token := self.expect(r'.'))
        ):
            return [token , self . _stre_level . pop ( )] [0] if + token == self . _stre_level [- 1] else None;
        self._reset(mark)
        return None;

    @memoize
    def simple_str_char(self) -> Optional[Any]:
        # simple_str_char: '\\\\\n' | '\\\\\\\\' | '\\\\\47' | '\\\\\42' | '\\\\a' | '\\\\b' | '\\\\t' | '\\\\n' | '\\\\v' | '\\\\f' | '\\\\r' | '\\\\[0-7][0-7]?[0-7]?' | '\\\\x[0-9a-fA-F]{2}' | '\\\\N\\{[^}]*\\}' | '\\\\u[0-9a-fA-F]{4}' | '\\\\U[0-9a-fA-F]{8}' | '\\\\.' | r'.'
        mark = self._mark()
        if (
            (token := self.expect('\\\\\n'))
        ):
            return token ( '' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\\\\\'))
        ):
            return token ( '\\' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\\47'))
        ):
            return token ( "\47" );
        self._reset(mark)
        if (
            (token := self.expect('\\\\\42'))
        ):
            return token ( '\42' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\a'))
        ):
            return token ( '\7' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\b'))
        ):
            return token ( '\10' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\t'))
        ):
            return token ( '\11' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\n'))
        ):
            return token ( '\12' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\v'))
        ):
            return token ( '\13' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\f'))
        ):
            return token ( '\14' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\r'))
        ):
            return token ( '\15' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\[0-7][0-7]?[0-7]?'))
        ):
            return token ( chr ( int ( token ( ) [1 :] , 8 ) ) );
        self._reset(mark)
        if (
            (token := self.expect('\\\\x[0-9a-fA-F]{2}'))
        ):
            return token ( chr ( int ( token ( ) [2 :] , 16 ) ) );
        self._reset(mark)
        if (
            (token := self.expect('\\\\N\\{[^}]*\\}'))
        ):
            return [None if c is None else token ( c ) for c in [self . _unicode_lookup ( token ( ) [3 : - 1] )]] [0];
        self._reset(mark)
        if (
            (token := self.expect('\\\\u[0-9a-fA-F]{4}'))
        ):
            return token ( chr ( int ( token ( ) [2 :] , 16 ) ) );
        self._reset(mark)
        if (
            (token := self.expect('\\\\U[0-9a-fA-F]{8}'))
        ):
            return [token ( chr ( c ) ) if c < 0x110000 else None for c in [int ( token ( ) [2 :] , 16 )]] [0];
        self._reset(mark)
        if (
            (token := self.expect('\\\\.'))
        ):
            return token;
        self._reset(mark)
        if (
            (token := self.expect(r'.'))
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def simple_str_end(self) -> Optional[Any]:
        # simple_str_end: stre | simple_str_char simple_str_end
        mark = self._mark()
        if (
            (a := self.stre())
        ):
            return a ( '' );
        self._reset(mark)
        if (
            (a := self.simple_str_char())
            and
            (b := self.simple_str_end())
        ):
            return a + b;
        self._reset(mark)
        return None;

    @memoize
    def simple_str(self) -> Optional[Any]:
        # simple_str: '\x27' start_stre_a simple_str_end | '\x22' start_stre_q simple_str_end
        mark = self._mark()
        if (
            (token := self.expect('\x27'))
            and
            (self.start_stre_a())
            and
            (data := self.simple_str_end())
        ):
            return token ( data );
        self._reset(mark)
        if (
            (token := self.expect('\x22'))
            and
            (self.start_stre_q())
            and
            (data := self.simple_str_end())
        ):
            return token ( data );
        self._reset(mark)
        return None;

    @memoize
    def raw_str_char(self) -> Optional[Any]:
        # raw_str_char: r'.'
        mark = self._mark()
        if (
            (token := self.expect(r'.'))
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def raw_str_end(self) -> Optional[Any]:
        # raw_str_end: stre | raw_str_char raw_str_end
        mark = self._mark()
        if (
            (a := self.stre())
        ):
            return a ( '' );
        self._reset(mark)
        if (
            (a := self.raw_str_char())
            and
            (b := self.raw_str_end())
        ):
            return a + b;
        self._reset(mark)
        return None;

    @memoize
    def raw_str(self) -> Optional[Any]:
        # raw_str: '\x27' start_stre_a raw_str_end | '\x22' start_stre_q raw_str_end
        mark = self._mark()
        if (
            (token := self.expect('\x27'))
            and
            (self.start_stre_a())
            and
            (data := self.raw_str_end())
        ):
            return token ( data );
        self._reset(mark)
        if (
            (token := self.expect('\x22'))
            and
            (self.start_stre_q())
            and
            (data := self.raw_str_end())
        ):
            return token ( data );
        self._reset(mark)
        return None;

    @memoize
    def formatted_str_char(self) -> Optional[Any]:
        # formatted_str_char: '\47' | r'\{\{' | r'\}\}' | r'\{\}' | r'\{' spaces f_expression '![ras]'? (r':' start_stre_b formatted_str_end | r'\}') | simple_str_char
        mark = self._mark()
        if (
            (a := self.expect('\47'))
        ):
            return a ( '' );
        self._reset(mark)
        if (
            (a := self.expect(r'\{\{'))
        ):
            return a ( '\173' );
        self._reset(mark)
        if (
            (a := self.expect(r'\}\}'))
        ):
            return a ( '\175' );
        self._reset(mark)
        if (
            (self.expect(r'\{\}'))
        ):
            return None;
        self._reset(mark)
        if (
            (self.expect(r'\{'))
            and
            (self.spaces())
            and
            (value := self.f_expression())
            and
            (conversion := self.expect('![ras]'),)
            and
            (format_spec := self._tmp_1())
        ):
            return ast . FormattedValue ( value = value , conversion = ord ( conversion ( ) [- 1] ) if conversion else - 1 , format_spec = None if isinstance ( format_spec , self . _token ) else ast . JoinedStr ( values = [values if not values or isinstance ( values [- 1] , ast . Constant ) else values + [ast . Constant ( value = '' )] for values in [+ format_spec [2]]] [0] ) , );
        self._reset(mark)
        if (
            (simple_str_char := self.simple_str_char())
        ):
            return simple_str_char;
        self._reset(mark)
        return None;

    @memoize
    def formatted_str_end(self) -> Optional[Any]:
        # formatted_str_end: stre | formatted_str_char formatted_str_end
        mark = self._mark()
        if (
            (self.stre())
        ):
            return self . _make_true ( [] );
        self._reset(mark)
        if (
            (a := self.formatted_str_char())
            and
            (b := self.formatted_str_end())
        ):
            return self . _make_true ( [a] + ( + b ) if not isinstance ( a , self . _token ) else [ast . Constant ( token = a , value = + a + ( + b ) [0] . value )] + ( + b ) [1 :] if + b and isinstance ( ( + b ) [0] , ast . Constant ) else [ast . Constant ( token = a , value = + a )] + ( + b ) );
        self._reset(mark)
        return None;

    @memoize
    def formatted_str(self) -> Optional[Any]:
        # formatted_str: '\x27' start_stre_a formatted_str_end | '\x22' start_stre_q formatted_str_end
        mark = self._mark()
        if (
            (self.expect('\x27'))
            and
            (self.start_stre_a())
            and
            (data := self.formatted_str_end())
        ):
            return data;
        self._reset(mark)
        if (
            (self.expect('\x22'))
            and
            (self.start_stre_q())
            and
            (data := self.formatted_str_end())
        ):
            return data;
        self._reset(mark)
        return None;

    @memoize
    def raw_formatted_str_char(self) -> Optional[Any]:
        # raw_formatted_str_char: '\47' | r'\{\{' | r'\}\}' | r'\{\}' | r'\{' spaces f_expression '![ras]'? (r':' start_stre_b formatted_str_end | r'\}') | raw_str_char
        mark = self._mark()
        if (
            (a := self.expect('\47'))
        ):
            return a ( '' );
        self._reset(mark)
        if (
            (a := self.expect(r'\{\{'))
        ):
            return a ( '\173' );
        self._reset(mark)
        if (
            (a := self.expect(r'\}\}'))
        ):
            return a ( '\175' );
        self._reset(mark)
        if (
            (self.expect(r'\{\}'))
        ):
            return None;
        self._reset(mark)
        if (
            (self.expect(r'\{'))
            and
            (self.spaces())
            and
            (value := self.f_expression())
            and
            (conversion := self.expect('![ras]'),)
            and
            (format_spec := self._tmp_2())
        ):
            return ast . FormattedValue ( value = value , conversion = ord ( conversion ( ) [- 1] ) if conversion else - 1 , format_spec = None if isinstance ( format_spec , self . _token ) else ast . JoinedStr ( values = [values if not values or isinstance ( values [- 1] , ast . Constant ) else values + [ast . Constant ( value = '' )] for values in [+ format_spec [2]]] [0] ) , );
        self._reset(mark)
        if (
            (raw_str_char := self.raw_str_char())
        ):
            return raw_str_char;
        self._reset(mark)
        return None;

    @memoize
    def raw_formatted_str_end(self) -> Optional[Any]:
        # raw_formatted_str_end: stre | raw_formatted_str_char raw_formatted_str_end
        mark = self._mark()
        if (
            (self.stre())
        ):
            return self . _make_true ( [] );
        self._reset(mark)
        if (
            (a := self.raw_formatted_str_char())
            and
            (b := self.raw_formatted_str_end())
        ):
            return self . _make_true ( [a] + ( + b ) if not isinstance ( a , self . _token ) else [ast . Constant ( token = a , value = + a + ( + b ) [0] . value )] + ( + b ) [1 :] if + b and isinstance ( ( + b ) [0] , ast . Constant ) else [ast . Constant ( token = a , value = + a )] + ( + b ) );
        self._reset(mark)
        return None;

    @memoize
    def raw_formatted_str(self) -> Optional[Any]:
        # raw_formatted_str: '\x27' start_stre_a raw_formatted_str_end | '\x22' start_stre_q raw_formatted_str_end
        mark = self._mark()
        if (
            (self.expect('\x27'))
            and
            (self.start_stre_a())
            and
            (data := self.raw_formatted_str_end())
        ):
            return data;
        self._reset(mark)
        if (
            (self.expect('\x22'))
            and
            (self.start_stre_q())
            and
            (data := self.raw_formatted_str_end())
        ):
            return data;
        self._reset(mark)
        return None;

    @memoize
    def byte_str_char(self) -> Optional[Any]:
        # byte_str_char: '\\\\\n' | '\\\\\\\\' | '\\\\\47' | '\\\\\42' | '\\\\a' | '\\\\b' | '\\\\t' | '\\\\n' | '\\\\v' | '\\\\f' | '\\\\r' | '\\\\[0-7][0-7]?[0-7]?' | '\\\\x[0-9a-fA-F]{2}' | '\\\\.' | r'.'
        mark = self._mark()
        if (
            (token := self.expect('\\\\\n'))
        ):
            return token ( b'' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\\\\\'))
        ):
            return token ( b'\\' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\\47'))
        ):
            return token ( b'\47' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\\42'))
        ):
            return token ( b'\42' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\a'))
        ):
            return token ( b'\7' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\b'))
        ):
            return token ( b'\10' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\t'))
        ):
            return token ( b'\11' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\n'))
        ):
            return token ( b'\12' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\v'))
        ):
            return token ( b'\13' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\f'))
        ):
            return token ( b'\14' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\r'))
        ):
            return token ( b'\15' );
        self._reset(mark)
        if (
            (token := self.expect('\\\\[0-7][0-7]?[0-7]?'))
        ):
            return token ( bytes ( [int ( token ( ) [1 :] , 8 ) % 256] ) );
        self._reset(mark)
        if (
            (token := self.expect('\\\\x[0-9a-fA-F]{2}'))
        ):
            return token ( bytes ( [int ( token ( ) [2 :] , 16 ) % 256] ) );
        self._reset(mark)
        if (
            (token := self.expect('\\\\.'))
        ):
            return token ( token ( ) . encode ( ) ) if len ( token ( ) [1] . encode ( ) ) < 2 else None;
        self._reset(mark)
        if (
            (token := self.expect(r'.'))
        ):
            return token ( token ( ) . encode ( ) ) if len ( token ( ) [0] . encode ( ) ) < 2 else None;
        self._reset(mark)
        return None;

    @memoize
    def byte_str_end(self) -> Optional[Any]:
        # byte_str_end: stre | byte_str_char byte_str_end
        mark = self._mark()
        if (
            (a := self.stre())
        ):
            return a ( b'' );
        self._reset(mark)
        if (
            (a := self.byte_str_char())
            and
            (b := self.byte_str_end())
        ):
            return a + b;
        self._reset(mark)
        return None;

    @memoize
    def byte_str(self) -> Optional[Any]:
        # byte_str: '\x27' start_stre_a byte_str_end | '\x22' start_stre_q byte_str_end
        mark = self._mark()
        if (
            (token := self.expect('\x27'))
            and
            (self.start_stre_a())
            and
            (data := self.byte_str_end())
        ):
            return token ( data );
        self._reset(mark)
        if (
            (token := self.expect('\x22'))
            and
            (self.start_stre_q())
            and
            (data := self.byte_str_end())
        ):
            return token ( data );
        self._reset(mark)
        return None;

    @memoize
    def raw_byte_str_char(self) -> Optional[Any]:
        # raw_byte_str_char: r'.'
        mark = self._mark()
        if (
            (token := self.expect(r'.'))
        ):
            return token ( token ( ) . encode ( ) ) if len ( token ( ) [0] . encode ( ) ) < 2 else None;
        self._reset(mark)
        return None;

    @memoize
    def raw_byte_str_end(self) -> Optional[Any]:
        # raw_byte_str_end: stre | raw_byte_str_char raw_byte_str_end
        mark = self._mark()
        if (
            (a := self.stre())
        ):
            return a ( b'' );
        self._reset(mark)
        if (
            (a := self.raw_byte_str_char())
            and
            (b := self.raw_byte_str_end())
        ):
            return a + b;
        self._reset(mark)
        return None;

    @memoize
    def raw_byte_str(self) -> Optional[Any]:
        # raw_byte_str: '\x27' start_stre_a raw_byte_str_end | '\x22' start_stre_q raw_byte_str_end
        mark = self._mark()
        if (
            (token := self.expect('\x27'))
            and
            (self.start_stre_a())
            and
            (data := self.raw_byte_str_end())
        ):
            return token ( data );
        self._reset(mark)
        if (
            (token := self.expect('\x22'))
            and
            (self.start_stre_q())
            and
            (data := self.raw_byte_str_end())
        ):
            return token ( data );
        self._reset(mark)
        return None;

    @memoize
    def str_with_prefix(self) -> Optional[Any]:
        # str_with_prefix: r'[Uu]?' simple_str | r'[fF]' formatted_str | r'[rR]' raw_str | r'[Ff][Rr]|[Rr][Ff]' raw_formatted_str | r'[Bb]' byte_str | r'[Rr][Bb]|[Bb][Rr]' raw_byte_str
        mark = self._mark()
        if (
            (token := self.expect(r'[Uu]?'))
            and
            (data := self.simple_str())
        ):
            return ast . Constant ( token = token , value = + data , kind = + token if + token else None );
        self._reset(mark)
        if (
            (token := self.expect(r'[fF]'))
            and
            (values := self.formatted_str())
        ):
            return ast . JoinedStr ( token = token , values = + values );
        self._reset(mark)
        if (
            (token := self.expect(r'[rR]'))
            and
            (data := self.raw_str())
        ):
            return ast . Constant ( token = token , value = + data );
        self._reset(mark)
        if (
            (token := self.expect(r'[Ff][Rr]|[Rr][Ff]'))
            and
            (self.raw_formatted_str())
        ):
            return ast . JoinedStr ( token = token , values = + values );
        self._reset(mark)
        if (
            (token := self.expect(r'[Bb]'))
            and
            (data := self.byte_str())
        ):
            return ast . Constant ( token = token , value = + data );
        self._reset(mark)
        if (
            (token := self.expect(r'[Rr][Bb]|[Bb][Rr]'))
            and
            (data := self.raw_byte_str())
        ):
            return ast . Constant ( token = token , value = + data );
        self._reset(mark)
        return None;

    @memoize
    def stringliteral(self) -> Optional[Any]:
        # stringliteral: str_with_prefix spaces
        mark = self._mark()
        if (
            (token := self.str_with_prefix())
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def integer(self) -> Optional[Any]:
        # integer: r'[1-9][_0-9]*' | r'0[bB][_01]+' | r'0[oO][_0-7]+' | r'0[xX][_0-9a-fA-f]+' | r'0[_0]*'
        mark = self._mark()
        if (
            (literal := self.expect(r'[1-9][_0-9]*'))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect(r'0[bB][_01]+'))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect(r'0[oO][_0-7]+'))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect(r'0[xX][_0-9a-fA-f]+'))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect(r'0[_0]*'))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def digitpart(self) -> Optional[Any]:
        # digitpart: r'[0-9][_0-9]*'
        mark = self._mark()
        if (
            (literal := self.expect(r'[0-9][_0-9]*'))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def pointfloat(self) -> Optional[Any]:
        # pointfloat: digitpart? r'\.' digitpart | digitpart r'\.'
        mark = self._mark()
        if (
            (a := self.digitpart(),)
            and
            (b := self.expect(r'\.'))
            and
            (c := self.digitpart())
        ):
            return a + b + c;
        self._reset(mark)
        if (
            (a := self.digitpart())
            and
            (b := self.expect(r'\.'))
        ):
            return a + b;
        self._reset(mark)
        return None;

    @memoize
    def exponentfloat(self) -> Optional[Any]:
        # exponentfloat: (digitpart | pointfloat) r'[eE][+-]?' digitpart
        mark = self._mark()
        if (
            (a := self._tmp_3())
            and
            (b := self.expect(r'[eE][+-]?'))
            and
            (c := self.digitpart())
        ):
            return a + b + c;
        self._reset(mark)
        return None;

    @memoize
    def floatnumber(self) -> Optional[Any]:
        # floatnumber: pointfloat | exponentfloat
        mark = self._mark()
        if (
            (pointfloat := self.pointfloat())
        ):
            return pointfloat;
        self._reset(mark)
        if (
            (exponentfloat := self.exponentfloat())
        ):
            return exponentfloat;
        self._reset(mark)
        return None;

    @memoize
    def imagnumber(self) -> Optional[Any]:
        # imagnumber: (floatnumber | digitpart) r'[jJ]'
        mark = self._mark()
        if (
            (a := self._tmp_4())
            and
            (b := self.expect(r'[jJ]'))
        ):
            return a + b;
        self._reset(mark)
        return None;

    @memoize
    def literal(self) -> Optional[Any]:
        # literal: stringliteral | imagnumber spaces | floatnumber spaces | integer spaces
        mark = self._mark()
        if (
            (stringliteral := self.stringliteral())
        ):
            return stringliteral;
        self._reset(mark)
        if (
            (token := self.imagnumber())
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = complex ( + token ) );
        self._reset(mark)
        if (
            (token := self.floatnumber())
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = float ( + token ) );
        self._reset(mark)
        if (
            (token := self.integer())
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = int ( + token , 0 ) );
        self._reset(mark)
        return None;

    @memoize
    def constant(self) -> Optional[Any]:
        # constant: r'None' spaces | r'\.\.\.' spaces | r'False' spaces | r'True' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'None'))
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = None );
        self._reset(mark)
        if (
            (token := self.expect(r'\.\.\.'))
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = ... );
        self._reset(mark)
        if (
            (token := self.expect(r'False'))
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = False );
        self._reset(mark)
        if (
            (token := self.expect(r'True'))
            and
            (self.spaces())
        ):
            return ast . Constant ( token = token , value = True );
        self._reset(mark)
        return None;

    @memoize
    def identifier(self) -> Optional[Any]:
        # identifier: r'[^\W0-9]\w*' spaces
        mark = self._mark()
        if (
            (token := self.expect(r'[^\W0-9]\w*'))
            and
            (self.spaces())
        ):
            return token;
        self._reset(mark)
        return None;

    @memoize
    def get_name(self) -> Optional[Any]:
        # get_name: identifier
        mark = self._mark()
        if (
            (token := self.identifier())
        ):
            return ast . Name ( token = token , id = + token , ctx = ast . Load ( token = token ) );
        self._reset(mark)
        return None;

    @memoize
    def set_name(self) -> Optional[Any]:
        # set_name: identifier
        mark = self._mark()
        if (
            (token := self.identifier())
        ):
            return ast . Name ( token = token , id = + token , ctx = ast . Store ( token = token ) );
        self._reset(mark)
        return None;

    @memoize
    def del_name(self) -> Optional[Any]:
        # del_name: identifier
        mark = self._mark()
        if (
            (token := self.identifier())
        ):
            return ast . Name ( token = token , id = + token , ctx = ast . Del ( token = token ) );
        self._reset(mark)
        return None;

    @logger
    def get_attributeref(self) -> Optional[Any]:
        # get_attributeref: primary dot_token identifier
        mark = self._mark()
        if (
            (value := self.primary())
            and
            (self.dot_token())
            and
            (attr := self.identifier())
        ):
            return ast . Attribute ( token = value . token , value = value , attr = + attr , ctx = ast . Load ( token = value . token ) );
        self._reset(mark)
        return None;

    @memoize
    def set_attributeref(self) -> Optional[Any]:
        # set_attributeref: primary
        mark = self._mark()
        if (
            (value := self.primary())
        ):
            return ast . Attribute ( token = value . token , value = value . value , attr = value . attr , ctx = ast . Store ( token = value . token ) ) if isinstance ( value , ast . Attribute ) else None;
        self._reset(mark)
        return None;

    @memoize
    def del_attributeref(self) -> Optional[Any]:
        # del_attributeref: primary
        mark = self._mark()
        if (
            (value := self.primary())
        ):
            return ast . Attribute ( token = value . token , value = value . value , attr = value . attr , ctx = ast . Del ( token = value . token ) ) if isinstance ( value , ast . Attribute ) else None;
        self._reset(mark)
        return None;

    @memoize
    def sub_item(self) -> Optional[Any]:
        # sub_item: expression? colon_token expression? [colon_token expression?] | expression
        mark = self._mark()
        if (
            (lower := self.expression(),)
            and
            (token := self.colon_token())
            and
            (upper := self.expression(),)
            and
            (step := self._tmp_5(),)
        ):
            return ast . Slice ( token = token , lower = lower , upper = upper , step = step [1] if step else None );
        self._reset(mark)
        if (
            (expression := self.expression())
        ):
            return expression;
        self._reset(mark)
        return None;

    @logger
    def get_subscription(self) -> Optional[Any]:
        # get_subscription: primary left_bracket_token comma_token.sub_item+ comma_token? right_bracket_token
        mark = self._mark()
        if (
            (value := self.primary())
            and
            (self.left_bracket_token())
            and
            (slices := self._gather_6())
            and
            (tc := self.comma_token(),)
            and
            (self.right_bracket_token())
        ):
            return ast . Subscript ( token = value . token , value = value , slice = ast . Tuple ( token = value . token , elts = slices , ctx = ast . Load ( token = value . token ) ) if len ( slices ) != 1 or tc else slices [0] , ctx = ast . Load ( token = value . token ) );
        self._reset(mark)
        return None;

    @memoize
    def set_subscription(self) -> Optional[Any]:
        # set_subscription: primary
        mark = self._mark()
        if (
            (value := self.primary())
        ):
            return ast . Subscript ( token = value . token , value = value . value , slice = value . slice , ctx = ast . Store ( token = value . token ) ) if isinstance ( value , ast . Subscript ) else None;
        self._reset(mark)
        return None;

    @memoize
    def del_subscription(self) -> Optional[Any]:
        # del_subscription: primary
        mark = self._mark()
        if (
            (value := self.primary())
        ):
            return ast . Subscript ( token = value . token , value = value . value , slice = value . slice , ctx = ast . Del ( token = value . token ) ) if isinstance ( value , ast . Subscript ) else None;
        self._reset(mark)
        return None;

    @memoize
    def arithmetic_parentheses(self) -> Optional[Any]:
        # arithmetic_parentheses: left_paren_token yield_expression right_paren_token | left_paren_token walrus_expression right_paren_token
        mark = self._mark()
        if (
            (self.left_paren_token())
            and
            (value := self.yield_expression())
            and
            (self.right_paren_token())
        ):
            return value;
        self._reset(mark)
        if (
            (self.left_paren_token())
            and
            (value := self.walrus_expression())
            and
            (self.right_paren_token())
        ):
            return value;
        self._reset(mark)
        return None;

    @memoize
    def comp_for(self) -> Optional[Any]:
        # comp_for: ((async_token? for_token set_multiple_targets in_token ready_to_if_expr ((if_token ready_to_if_expr))*))+
        mark = self._mark()
        if (
            (a := self._loop1_8())
        ):
            return self . _make_true ( [ast . comprehension ( token = a [1] , target = a [2] , iter = a [4] , ifs = [a [1] for a in a [5]] if a [5] else [] , is_async = + bool ( a [0] ) , ) if not a [0] or self . _func_level [- 1] == 2 else self . _error ( ) for a in a] );
        self._reset(mark)
        return None;

    @memoize
    def display_item(self) -> Optional[Any]:
        # display_item: walrus_expression | star_token ready_to_be_starred_expr
        mark = self._mark()
        if (
            (walrus_expression := self.walrus_expression())
        ):
            return walrus_expression;
        self._reset(mark)
        if (
            (self.star_token())
            and
            (value := self.ready_to_be_starred_expr())
        ):
            return ast . Starred ( token = value . token , value = value , ctx = ast . Load ( token = value . token ) );
        self._reset(mark)
        return None;

    @memoize
    def display_content(self) -> Optional[Any]:
        # display_content: walrus_expression comp_for | comma_token.display_item+ comma_token?
        mark = self._mark()
        if (
            (walrus_expression := self.walrus_expression())
            and
            (comp_for := self.comp_for())
        ):
            return [walrus_expression, comp_for];
        self._reset(mark)
        if (
            (_gather_9 := self._gather_9())
            and
            (opt := self.comma_token(),)
        ):
            return [_gather_9, opt];
        self._reset(mark)
        return None;

    @memoize
    def tuple_display(self) -> Optional[Any]:
        # tuple_display: left_paren_token display_content? right_paren_token
        mark = self._mark()
        if (
            (token := self.left_paren_token())
            and
            (content := self.display_content(),)
            and
            (self.right_paren_token())
        ):
            return None if content and not content [1] and len ( content [0] ) == 1 else ast . GeneratorExpr ( token = token , elt = content [0] , generators = + content [1] ) if content and content [1] and + content [1] != ',' else ast . Tuple ( token = token , elts = content [0] if content else [] , ctx = ast . Load ( token = token ) );
        self._reset(mark)
        return None;

    @memoize
    def list_display(self) -> Optional[Any]:
        # list_display: left_bracket_token display_content? right_bracket_token
        mark = self._mark()
        if (
            (token := self.left_bracket_token())
            and
            (content := self.display_content(),)
            and
            (self.right_bracket_token())
        ):
            return ast . ListComp ( token = token , elt = content [0] , generators = + content [1] ) if content and content [1] and + content [1] != ',' else ast . List ( token = token , elts = content [0] if content else [] , ctx = ast . Load ( token = token ) );
        self._reset(mark)
        return None;

    @memoize
    def set_display(self) -> Optional[Any]:
        # set_display: left_brace_token display_content right_brace_token
        mark = self._mark()
        if (
            (token := self.left_brace_token())
            and
            (content := self.display_content())
            and
            (self.right_brace_token())
        ):
            return ast . SetComp ( token , elt = content [0] , generators = + content [1] ) if content [1] and + content [1] != ',' else ast . Set ( token , elts = content [0] if content else [] );
        self._reset(mark)
        return None;

    @memoize
    def dict_display_item(self) -> Optional[Any]:
        # dict_display_item: expression colon_token expression | starstar_token ready_to_be_starred_expr
        mark = self._mark()
        if (
            (key := self.expression())
            and
            (self.colon_token())
            and
            (value := self.expression())
        ):
            return [key , value];
        self._reset(mark)
        if (
            (self.starstar_token())
            and
            (value := self.ready_to_be_starred_expr())
        ):
            return [None , value];
        self._reset(mark)
        return None;

    @memoize
    def dict_display_content(self) -> Optional[Any]:
        # dict_display_content: expression colon_token expression comp_for | comma_token.dict_display_item+ comma_token?
        mark = self._mark()
        if (
            (expression := self.expression())
            and
            (colon_token := self.colon_token())
            and
            (expression_1 := self.expression())
            and
            (comp_for := self.comp_for())
        ):
            return [expression, colon_token, expression_1, comp_for];
        self._reset(mark)
        if (
            (_gather_11 := self._gather_11())
            and
            (opt := self.comma_token(),)
        ):
            return [_gather_11, opt];
        self._reset(mark)
        return None;

    @memoize
    def dict_display(self) -> Optional[Any]:
        # dict_display: left_brace_token dict_display_content? right_brace_token
        mark = self._mark()
        if (
            (token := self.left_brace_token())
            and
            (content := self.dict_display_content(),)
            and
            (self.right_brace_token())
        ):
            return ast . Dict ( token = token , keys = [] , values = [] ) if not content else ast . Dict ( token = token , ** dict ( zip ( 'keys values' . split ( ) , map ( list , zip ( * content [0] ) ) ) ) ) if len ( content ) == 2 else ast . DictComp ( token = token , key = content [0] , value = content [2] , generators = + content [3] );
        self._reset(mark)
        return None;

    @memoize
    def call_arg(self) -> Optional[Any]:
        # call_arg: identifier assign_token expression | starstar_token expression | star_token expression | walrus_expression
        mark = self._mark()
        if (
            (arg := self.identifier())
            and
            (self.assign_token())
            and
            (value := self.expression())
        ):
            return ast . keyword ( token = arg , arg = + arg , value = value );
        self._reset(mark)
        if (
            (self.starstar_token())
            and
            (value := self.expression())
        ):
            return ast . keyword ( token = value . token , value = value );
        self._reset(mark)
        if (
            (self.star_token())
            and
            (value := self.expression())
        ):
            return ast . Starred ( token = value . token , value = value , ctx = ast . Load ( token = value . token ) );
        self._reset(mark)
        if (
            (walrus_expression := self.walrus_expression())
        ):
            return walrus_expression;
        self._reset(mark)
        return None;

    @memoize
    def call_args(self) -> Optional[Any]:
        # call_args: '' [comma_token.call_arg+ comma_token?]
        mark = self._mark()
        if (
            (self.expect(''))
            and
            (args := self._tmp_13(),)
        ):
            return dict ( args = [] , keywords = [] ) if not args else dict ( args = [arg for arg in args [0] if type ( arg ) != ast . keyword] , keywords = [arg for arg in args [0] if type ( arg ) == ast . keyword] ) if not any ( [isinstance ( arg1 , ast . keyword ) and not isinstance ( arg2 , ast . keyword | ast . Starred ) or isinstance ( arg1 , ast . keyword ) and arg1 . arg == None and not isinstance ( arg2 , ast . keyword ) for n , arg2 in enumerate ( args [0] ) for arg1 in args [0] [: n]] ) else None;
        self._reset(mark)
        return None;

    @logger
    def call(self) -> Optional[Any]:
        # call: primary left_paren_token (walrus_expression comp_for | call_args) right_paren_token
        mark = self._mark()
        if (
            (func := self.primary())
            and
            (self.left_paren_token())
            and
            (args := self._tmp_14())
            and
            (self.right_paren_token())
        ):
            return ast . Call ( token = func . token , func = func , args = [ast . GeneratorExp ( token = func . token , elt = args [0] , generators = + args [1] )] , keywords = [] ) if isinstance ( args , list ) else ast . Call ( token = func . token , func = func , args = args ['args'] , keywords = args ['keywords'] );
        self._reset(mark)
        return None;

    @memoize_left_rec
    def primary(self) -> Optional[Any]:
        # primary: get_attributeref | get_subscription | call | tuple_display | list_display | dict_display | set_display | literal | constant | get_name | arithmetic_parentheses
        mark = self._mark()
        if (
            (get_attributeref := self.get_attributeref())
        ):
            return get_attributeref;
        self._reset(mark)
        if (
            (get_subscription := self.get_subscription())
        ):
            return get_subscription;
        self._reset(mark)
        if (
            (call := self.call())
        ):
            return call;
        self._reset(mark)
        if (
            (tuple_display := self.tuple_display())
        ):
            return tuple_display;
        self._reset(mark)
        if (
            (list_display := self.list_display())
        ):
            return list_display;
        self._reset(mark)
        if (
            (dict_display := self.dict_display())
        ):
            return dict_display;
        self._reset(mark)
        if (
            (set_display := self.set_display())
        ):
            return set_display;
        self._reset(mark)
        if (
            (literal := self.literal())
        ):
            return literal;
        self._reset(mark)
        if (
            (constant := self.constant())
        ):
            return constant;
        self._reset(mark)
        if (
            (get_name := self.get_name())
        ):
            return get_name;
        self._reset(mark)
        if (
            (arithmetic_parentheses := self.arithmetic_parentheses())
        ):
            return arithmetic_parentheses;
        self._reset(mark)
        return None;

    @memoize
    def await_expr(self) -> Optional[Any]:
        # await_expr: await_token? primary
        mark = self._mark()
        if (
            (new_node := self.await_token(),)
            and
            (value := self.primary())
        ):
            return ast . Await ( token = value . token , value = value ) if new_node else value;
        self._reset(mark)
        return None;

    @memoize
    def power(self) -> Optional[Any]:
        # power: (await_expr) [starstar_token u_expr]
        mark = self._mark()
        if (
            (left := self.await_expr())
            and
            (new_node := self._tmp_15(),)
        ):
            return ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ new_node [0]] , right = new_node [1] ) if new_node else left;
        self._reset(mark)
        return None;

    @memoize
    def u_expr(self) -> Optional[Any]:
        # u_expr: power | u_token u_expr
        mark = self._mark()
        if (
            (power := self.power())
        ):
            return power;
        self._reset(mark)
        if (
            (op := self.u_token())
            and
            (operand := self.u_expr())
        ):
            return ast . UnaryOp ( token = operand . token , operand = operand , op = self . _un_op_to_ast [+ op] );
        self._reset(mark)
        return None;

    @memoize
    def m_expr(self) -> Optional[Any]:
        # m_expr: u_expr ((m_token u_expr))*
        mark = self._mark()
        if (
            (left := self.u_expr())
            and
            (right := self._loop0_16(),)
        ):
            return self . _functools . reduce ( lambda left , right : ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ right [0]] , right = right [1] ) , right , left );
        self._reset(mark)
        return None;

    @memoize
    def a_expr(self) -> Optional[Any]:
        # a_expr: m_expr ((a_token m_expr))*
        mark = self._mark()
        if (
            (left := self.m_expr())
            and
            (right := self._loop0_17(),)
        ):
            return self . _functools . reduce ( lambda left , right : ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ right [0]] , right = right [1] ) , right , left );
        self._reset(mark)
        return None;

    @memoize
    def shift_expr(self) -> Optional[Any]:
        # shift_expr: a_expr ((shift_token a_expr))*
        mark = self._mark()
        if (
            (left := self.a_expr())
            and
            (right := self._loop0_18(),)
        ):
            return self . _functools . reduce ( lambda left , right : ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ right [0]] , right = right [1] ) , right , left );
        self._reset(mark)
        return None;

    @memoize
    def and_expr(self) -> Optional[Any]:
        # and_expr: shift_expr ((bit_and_token shift_expr))*
        mark = self._mark()
        if (
            (left := self.shift_expr())
            and
            (right := self._loop0_19(),)
        ):
            return self . _functools . reduce ( lambda left , right : ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ right [0]] , right = right [1] ) , right , left );
        self._reset(mark)
        return None;

    @memoize
    def xor_expr(self) -> Optional[Any]:
        # xor_expr: and_expr ((bit_xor_token and_expr))*
        mark = self._mark()
        if (
            (left := self.and_expr())
            and
            (right := self._loop0_20(),)
        ):
            return self . _functools . reduce ( lambda left , right : ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ right [0]] , right = right [1] ) , right , left );
        self._reset(mark)
        return None;

    @memoize
    def or_expr(self) -> Optional[Any]:
        # or_expr: xor_expr ((bit_or_token xor_expr))*
        mark = self._mark()
        if (
            (left := self.xor_expr())
            and
            (right := self._loop0_21(),)
        ):
            return self . _functools . reduce ( lambda left , right : ast . BinOp ( token = left . token , left = left , op = self . _bin_op_to_ast [+ right [0]] , right = right [1] ) , right , left );
        self._reset(mark)
        return None;

    @memoize
    def ready_to_be_starred_expr(self) -> Optional[Any]:
        # ready_to_be_starred_expr: or_expr
        mark = self._mark()
        if (
            (or_expr := self.or_expr())
        ):
            return or_expr;
        self._reset(mark)
        return None;

    @memoize
    def comp_operator(self) -> Optional[Any]:
        # comp_operator: comp_token | is_token not_token? | not_token? in_token
        mark = self._mark()
        if (
            (op := self.comp_token())
        ):
            return self . _bin_op_to_ast [+ op];
        self._reset(mark)
        if (
            (a := self.is_token())
            and
            (s := self.not_token(),)
        ):
            return ast . IsNot ( token = a ) if s else ast . Is ( token = a );
        self._reset(mark)
        if (
            (s := self.not_token(),)
            and
            (a := self.in_token())
        ):
            return ast . NotIn ( token = a ) if s else ast . In ( token = a );
        self._reset(mark)
        return None;

    @memoize
    def comparison(self) -> Optional[Any]:
        # comparison: ready_to_be_starred_expr ((comp_operator ready_to_be_starred_expr))*
        mark = self._mark()
        if (
            (left := self.ready_to_be_starred_expr())
            and
            (new_node := self._loop0_22(),)
        ):
            return ast . Compare ( token = left , left = left , ops = [op for op , comparator in new_node] , comparators = [comparator for op , comparator in new_node] , ) if new_node else left;
        self._reset(mark)
        return None;

    @memoize
    def not_test(self) -> Optional[Any]:
        # not_test: not_token not_test | comparison
        mark = self._mark()
        if (
            (self.not_token())
            and
            (operand := self.not_test())
        ):
            return ast . UnaryOp ( token = operand . token , op = ast . Not ( token = operand . token ) , operand = operand );
        self._reset(mark)
        if (
            (comparison := self.comparison())
        ):
            return comparison;
        self._reset(mark)
        return None;

    @memoize
    def and_test(self) -> Optional[Any]:
        # and_test: not_test ((and_token not_test))*
        mark = self._mark()
        if (
            (left := self.not_test())
            and
            (right := self._loop0_23(),)
        ):
            return ast . BoolOp ( token = left . token , op = ast . And ( token = left . token ) , values = [left] + [value [1] for value in right] ) if right else left;
        self._reset(mark)
        return None;

    @memoize
    def or_test(self) -> Optional[Any]:
        # or_test: and_test ((or_token and_test))*
        mark = self._mark()
        if (
            (left := self.and_test())
            and
            (right := self._loop0_24(),)
        ):
            return ast . BoolOp ( token = left . token , op = ast . Or ( token = left . token ) , values = [left] + [value [1] for value in right] ) if right else left;
        self._reset(mark)
        return None;

    @memoize
    def ready_to_if_expr(self) -> Optional[Any]:
        # ready_to_if_expr: or_test
        mark = self._mark()
        if (
            (or_test := self.or_test())
        ):
            return or_test;
        self._reset(mark)
        return None;

    @memoize
    def conditional_expression(self) -> Optional[Any]:
        # conditional_expression: ready_to_if_expr [if_token ready_to_if_expr else_token expression]
        mark = self._mark()
        if (
            (left := self.ready_to_if_expr())
            and
            (new_node := self._tmp_25(),)
        ):
            return ast . IfExp ( token = left . token , body = left , test = new_node [1] , orelse = new_node [3] ) if new_node else left;
        self._reset(mark)
        return None;

    @memoize
    def expression(self) -> Optional[Any]:
        # expression: lambda_token parameter_list_lmbd colon_token expression | conditional_expression
        mark = self._mark()
        if (
            (token := self.lambda_token())
            and
            (args := self.parameter_list_lmbd())
            and
            (self.colon_token())
            and
            (body := self.expression())
        ):
            return ast . Lambda ( token = token , args = args , body = body , );
        self._reset(mark)
        if (
            (conditional_expression := self.conditional_expression())
        ):
            return conditional_expression;
        self._reset(mark)
        return None;

    @memoize
    def tupled_expression(self) -> Optional[Any]:
        # tupled_expression: comma_token.expression+ comma_token?
        mark = self._mark()
        if (
            (vs := self._gather_26())
            and
            (tc := self.comma_token(),)
        ):
            return vs [0] if len ( vs ) == 1 and not tc else ast . Tuple ( token = vs [0] . token , elts = vs , ctx = ast . Load ( token = vs [0] . token ) );
        self._reset(mark)
        return None;

    @memoize
    def walrus_expression(self) -> Optional[Any]:
        # walrus_expression: [set_name walrus_token] expression
        mark = self._mark()
        if (
            (new_node := self._tmp_28(),)
            and
            (value := self.expression())
        ):
            return ast . NamedExpr ( token = value . token , target = new_node [0] , value = value ) if new_node else value;
        self._reset(mark)
        return None;

    @memoize
    def yield_expression(self) -> Optional[Any]:
        # yield_expression: yield_token from_token expression | yield_token tupled_expression? | tupled_expression
        mark = self._mark()
        if (
            (self.yield_token())
            and
            (self.from_token())
            and
            (value := self.expression())
        ):
            return ast . Yield ( token = value . token , value = value );
        self._reset(mark)
        if (
            (self.yield_token())
            and
            (value := self.tupled_expression(),)
        ):
            return ast . Yield ( token = value . token , value = value );
        self._reset(mark)
        if (
            (tupled_expression := self.tupled_expression())
        ):
            return tupled_expression;
        self._reset(mark)
        return None;

    @memoize
    def f_expression(self) -> Optional[Any]:
        # f_expression: yield_expression | conditional_expression
        mark = self._mark()
        if (
            (yield_expression := self.yield_expression())
        ):
            return yield_expression;
        self._reset(mark)
        if (
            (conditional_expression := self.conditional_expression())
        ):
            return conditional_expression;
        self._reset(mark)
        return None;

    @memoize
    def start_indent(self) -> Optional[Any]:
        # start_indent: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _indent_levels . append ( None ) , self . _update_indent ( )];
        self._reset(mark)
        return None;

    @memoize
    def stop_indent(self) -> Optional[Any]:
        # stop_indent: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _indent_levels . pop ( ) , self . _update_indent ( )];
        self._reset(mark)
        return None;

    @memoize
    def start_bracket(self) -> Optional[Any]:
        # start_bracket: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _bracket_level . append ( None )];
        self._reset(mark)
        return None;

    @memoize
    def stop_bracket(self) -> Optional[Any]:
        # stop_bracket: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _bracket_level . pop ( )];
        self._reset(mark)
        return None;

    @memoize
    def start_loop(self) -> Optional[Any]:
        # start_loop: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _loop_level . append ( 1 )];
        self._reset(mark)
        return None;

    @memoize
    def stop_loop(self) -> Optional[Any]:
        # stop_loop: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _loop_level . pop ( )];
        self._reset(mark)
        return None;

    @memoize
    def start_func(self) -> Optional[Any]:
        # start_func: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _loop_level . append ( 0 ) , self . _func_level . append ( 1 )];
        self._reset(mark)
        return None;

    @memoize
    def stop_func(self) -> Optional[Any]:
        # stop_func: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _loop_level . pop ( ) , self . _func_level . pop ( )];
        self._reset(mark)
        return None;

    @memoize
    def start_coro(self) -> Optional[Any]:
        # start_coro: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _loop_level . append ( 0 ) , self . _func_level . append ( 2 )];
        self._reset(mark)
        return None;

    @memoize
    def stop_coro(self) -> Optional[Any]:
        # stop_coro: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _loop_level . pop ( ) , self . _func_level . pop ( )];
        self._reset(mark)
        return None;

    @memoize
    def indent(self) -> Optional[Any]:
        # indent: spaces_for_indent
        mark = self._mark()
        if (
            (a := self.spaces_for_indent())
        ):
            return [self . _indent_levels . __setitem__ ( - 1 , + a ) , a] [1] if self . _indent_levels [- 1] == None and a ( ) != self . _indent_levels [- 2] and a ( ) . startswith ( self . _indent_levels [- 2] ) else [None , a] [self . _indent_levels [- 1] == + a];
        self._reset(mark)
        return None;

    @memoize
    def semicolon_separated_statements(self) -> Optional[Any]:
        # semicolon_separated_statements: (semicolon_token).simple_stmt+ semicolon_token? new_line
        mark = self._mark()
        if (
            (a := self._gather_29())
            and
            (self.semicolon_token(),)
            and
            (self.new_line())
        ):
            return self . _make_true ( a );
        self._reset(mark)
        return None;

    @memoize
    def line_content(self) -> Optional[Any]:
        # line_content: multiple_lines_statement | indent semicolon_separated_statements
        mark = self._mark()
        if (
            (a := self.multiple_lines_statement())
        ):
            return self . _make_true ( [a] );
        self._reset(mark)
        if (
            (self.indent())
            and
            (a := self.semicolon_separated_statements())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def line(self) -> Optional[Any]:
        # line: r'$' | spaces new_line | line_content
        mark = self._mark()
        if (
            (self.expect(r'$'))
        ):
            return None;
        self._reset(mark)
        if (
            (self.spaces())
            and
            (self.new_line())
        ):
            return self . _make_true ( [] );
        self._reset(mark)
        if (
            (a := self.line_content())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def code_block(self) -> Optional[Any]:
        # code_block: start_indent line+ stop_indent
        mark = self._mark()
        if (
            (self.start_indent())
            and
            (a := self._loop1_31())
            and
            (self.stop_indent())
        ):
            return self . _make_true ( [d for s in a for d in + s] );
        self._reset(mark)
        return None;

    @memoize
    def body(self) -> Optional[Any]:
        # body: semicolon_separated_statements | new_line code_block
        mark = self._mark()
        if (
            (semicolon_separated_statements := self.semicolon_separated_statements())
        ):
            return semicolon_separated_statements;
        self._reset(mark)
        if (
            (self.new_line())
            and
            (a := self.code_block())
        ):
            return a if + a else None;
        self._reset(mark)
        return None;

    @memoize
    def colon_and_body(self) -> Optional[Any]:
        # colon_and_body: colon_token body
        mark = self._mark()
        if (
            (self.colon_token())
            and
            (a := self.body())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def colon_and_loop_body(self) -> Optional[Any]:
        # colon_and_loop_body: start_loop colon_and_body stop_loop
        mark = self._mark()
        if (
            (self.start_loop())
            and
            (a := self.colon_and_body())
            and
            (self.stop_loop())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def colon_and_func_body(self) -> Optional[Any]:
        # colon_and_func_body: start_func colon_and_body stop_func
        mark = self._mark()
        if (
            (self.start_func())
            and
            (a := self.colon_and_body())
            and
            (self.stop_func())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def colon_and_coro_body(self) -> Optional[Any]:
        # colon_and_coro_body: start_coro colon_and_body stop_coro
        mark = self._mark()
        if (
            (self.start_coro())
            and
            (a := self.colon_and_body())
            and
            (self.stop_coro())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def start(self) -> Optional[Any]:
        # start: r'^' line* r'$'
        mark = self._mark()
        if (
            (token := self.expect(r'^'))
            and
            (a := self._loop0_32(),)
            and
            (self.expect(r'$'))
        ):
            return ast . Module ( token = token , body = [d for s in a for d in + s] , type_ignores = [] );
        self._reset(mark)
        return None;

    @memoize
    def multiple_lines_statement(self) -> Optional[Any]:
        # multiple_lines_statement: coro_def | func_def | async_for_stmt | for_stmt | while_stmt | if_stmt | try_stmt | with_stmt | async_with_stmt | class_def
        mark = self._mark()
        if (
            (coro_def := self.coro_def())
        ):
            return coro_def;
        self._reset(mark)
        if (
            (func_def := self.func_def())
        ):
            return func_def;
        self._reset(mark)
        if (
            (async_for_stmt := self.async_for_stmt())
        ):
            return async_for_stmt;
        self._reset(mark)
        if (
            (for_stmt := self.for_stmt())
        ):
            return for_stmt;
        self._reset(mark)
        if (
            (while_stmt := self.while_stmt())
        ):
            return while_stmt;
        self._reset(mark)
        if (
            (if_stmt := self.if_stmt())
        ):
            return if_stmt;
        self._reset(mark)
        if (
            (try_stmt := self.try_stmt())
        ):
            return try_stmt;
        self._reset(mark)
        if (
            (with_stmt := self.with_stmt())
        ):
            return with_stmt;
        self._reset(mark)
        if (
            (async_with_stmt := self.async_with_stmt())
        ):
            return async_with_stmt;
        self._reset(mark)
        if (
            (class_def := self.class_def())
        ):
            return class_def;
        self._reset(mark)
        return None;

    @memoize
    def if_stmt(self) -> Optional[Any]:
        # if_stmt: indent if_token walrus_expression colon_and_body ((indent elif_token walrus_expression colon_and_body))* [indent else_token colon_and_body]
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (token := self.if_token())
            and
            (test := self.walrus_expression())
            and
            (body := self.colon_and_body())
            and
            (mebbe := self._loop0_33(),)
            and
            (orelse := self._tmp_34(),)
        ):
            return self . _functools . reduce ( lambda a , s : [ast . If ( token = s [1] , test = s [2] , body = + s [3] , orelse = a )] , ( [[indent , token , test , body]] + ( mebbe if mebbe else [] ) ) [: : - 1] , + orelse [2] if orelse else [] ) [0];
        self._reset(mark)
        return None;

    @memoize
    def for_stmt(self) -> Optional[Any]:
        # for_stmt: indent for_token set_multiple_targets in_token expression colon_and_loop_body [indent else_token colon_and_body]
        mark = self._mark()
        if (
            (self.indent())
            and
            (token := self.for_token())
            and
            (target := self.set_multiple_targets())
            and
            (self.in_token())
            and
            (iter := self.expression())
            and
            (body := self.colon_and_loop_body())
            and
            (orelse := self._tmp_35(),)
        ):
            return ast . For ( token = token , target = target , iter = iter , body = + body , orelse = + orelse [2] if orelse else [] , );
        self._reset(mark)
        return None;

    @memoize
    def async_for_stmt(self) -> Optional[Any]:
        # async_for_stmt: indent async_token for_token set_multiple_targets in_token expression colon_and_loop_body [indent else_token colon_and_body]
        mark = self._mark()
        if (
            (self.indent())
            and
            (self.async_token())
            and
            (token := self.for_token())
            and
            (target := self.set_multiple_targets())
            and
            (self.in_token())
            and
            (iter := self.expression())
            and
            (body := self.colon_and_loop_body())
            and
            (orelse := self._tmp_36(),)
        ):
            return ast . AsyncFor ( token = token , target = target , iter = iter , body = + body , orelse = + orelse [2] if orelse else [] , ) if self . _func_level [- 1] == 2 else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def while_stmt(self) -> Optional[Any]:
        # while_stmt: indent while_token walrus_expression colon_and_loop_body [indent else_token colon_and_body]
        mark = self._mark()
        if (
            (self.indent())
            and
            (token := self.while_token())
            and
            (test := self.walrus_expression())
            and
            (body := self.colon_and_loop_body())
            and
            (orelse := self._tmp_37(),)
        ):
            return ast . While ( token = token , test = test , body = + body , orelse = + orelse [2] if orelse else [] , );
        self._reset(mark)
        return None;

    @memoize
    def try_stmt(self) -> Optional[Any]:
        # try_stmt: '' (try_group | try_except | try_finally)
        mark = self._mark()
        if (
            (self.expect(''))
            and
            (a := self._tmp_38())
        ):
            return ( ast . TryStar if a [3] and len ( a [3] [0] ) == 5 else ast . Try ) ( token = a [1] , body = + a [2] , handlers = [ast . ExceptHandler ( token = a [1] , type = handler [- 2] [0] if handler [- 2] else None , name = + handler [- 2] [1] [1] if handler [- 2] and handler [- 2] [1] else None , body = + handler [- 1] ) for handler in a [3]] , orelse = + a [4] [2] if a [4] else [] , finalbody = + a [5] [2] if a [5] else [] , );
        self._reset(mark)
        return None;

    @memoize
    def try_except(self) -> Optional[Any]:
        # try_except: indent try_token colon_and_body except_colon+ [indent else_token colon_and_body] [indent finally_token colon_and_body]
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (token := self.try_token())
            and
            (body := self.colon_and_body())
            and
            (handlers := self._loop1_39())
            and
            (orelse := self._tmp_40(),)
            and
            (finalbody := self._tmp_41(),)
        ):
            return [indent, token, body, handlers, orelse, finalbody];
        self._reset(mark)
        return None;

    @memoize
    def try_group(self) -> Optional[Any]:
        # try_group: indent try_token colon_and_body except_star+ [indent else_token colon_and_body] [indent finally_token colon_and_body]
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (token := self.try_token())
            and
            (body := self.colon_and_body())
            and
            (handlers := self._loop1_42())
            and
            (orelse := self._tmp_43(),)
            and
            (finalbody := self._tmp_44(),)
        ):
            return [indent, token, body, handlers, orelse, finalbody];
        self._reset(mark)
        return None;

    @memoize
    def try_finally(self) -> Optional[Any]:
        # try_finally: indent try_token colon_and_body (!'')* (!'')* (finally_token colon_and_body)
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (token := self.try_token())
            and
            (body := self.colon_and_body())
            and
            (handlers := self._loop0_45(),)
            and
            (orelse := self._loop0_46(),)
            and
            (finalbody := self._tmp_47())
        ):
            return [indent, token, body, handlers, orelse, finalbody];
        self._reset(mark)
        return None;

    @memoize
    def except_colon(self) -> Optional[Any]:
        # except_colon: indent except_token [expression [as_token identifier]] colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (q := self.except_token())
            and
            (w := self._tmp_48(),)
            and
            (e := self.colon_and_body())
        ):
            return [indent, q, w, e];
        self._reset(mark)
        return None;

    @memoize
    def except_star(self) -> Optional[Any]:
        # except_star: indent except_token star_token [expression [as_token identifier]] colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (except_token := self.except_token())
            and
            (star_token := self.star_token())
            and
            (opt := self._tmp_49(),)
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, except_token, star_token, opt, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def with_stmt_impl(self) -> Optional[Any]:
        # with_stmt_impl: with_token left_paren_token comma_token.(expression [as_token set_single_target])+ comma_token? right_paren_token colon_and_body | with_token comma_token.(expression [as_token set_single_target])+ colon_and_body
        mark = self._mark()
        if (
            (token := self.with_token())
            and
            (left_paren_token := self.left_paren_token())
            and
            (items := self._gather_50())
            and
            (opt := self.comma_token(),)
            and
            (right_paren_token := self.right_paren_token())
            and
            (body := self.colon_and_body())
        ):
            return [token, left_paren_token, items, opt, right_paren_token, body];
        self._reset(mark)
        if (
            (token := self.with_token())
            and
            (items := self._gather_52())
            and
            (body := self.colon_and_body())
        ):
            return [token, items, body];
        self._reset(mark)
        return None;

    @memoize
    def with_stmt(self) -> Optional[Any]:
        # with_stmt: indent with_stmt_impl
        mark = self._mark()
        if (
            (self.indent())
            and
            (a := self.with_stmt_impl())
        ):
            return ast . With ( token = a [0] , items = [ast . withitem ( token = a [0] , context_expr = item [0] , optional_vars = item [1] [1] [0] if item [1] else None ) for item in a [1]] , body = + a [2] , );
        self._reset(mark)
        return None;

    @memoize
    def async_with_stmt(self) -> Optional[Any]:
        # async_with_stmt: indent async_token with_stmt_impl
        mark = self._mark()
        if (
            (self.indent())
            and
            (self.async_token())
            and
            (a := self.with_stmt_impl())
        ):
            return ast . AsyncWith ( token = a [0] , items = [ast . withitem ( token = a [0] , context_expr = item [0] , optional_vars = item [1] [1] [0] if item [1] else None ) for item in a [1]] , body = + a [2] , ) if self . _func_level [- 1] == 2 else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def class_def(self) -> Optional[Any]:
        # class_def: decorator* indent class_token identifier type_params? [left_paren_token call_args right_paren_token] colon_and_body
        mark = self._mark()
        if (
            (decorator_list := self._loop0_54(),)
            and
            (self.indent())
            and
            (token := self.class_token())
            and
            (name := self.identifier())
            and
            (type_params := self.type_params(),)
            and
            (args := self._tmp_55(),)
            and
            (body := self.colon_and_body())
        ):
            return ast . ClassDef ( token = token , name = + name , bases = args [1] ['args'] if args else [] , keywords = args [1] ['keywords'] if args else [] , body = + body , decorator_list = decorator_list , type_params = + type_params if type_params else [] );
        self._reset(mark)
        return None;

    @memoize
    def parameter_with_colon(self) -> Optional[Any]:
        # parameter_with_colon: identifier colon_token expression
        mark = self._mark()
        if (
            (arg := self.identifier())
            and
            (self.colon_token())
            and
            (annotation := self.expression())
        ):
            return ast . arg ( token = arg , arg = + arg , annotation = annotation ) if self . _args_level [- 1] else None;
        self._reset(mark)
        return None;

    @memoize
    def parameter_without_colon(self) -> Optional[Any]:
        # parameter_without_colon: identifier
        mark = self._mark()
        if (
            (arg := self.identifier())
        ):
            return ast . arg ( token = arg , arg = + arg , annotation = None );
        self._reset(mark)
        return None;

    @memoize
    def parameter(self) -> Optional[Any]:
        # parameter: parameter_with_colon | parameter_without_colon
        mark = self._mark()
        if (
            (parameter_with_colon := self.parameter_with_colon())
        ):
            return parameter_with_colon;
        self._reset(mark)
        if (
            (parameter_without_colon := self.parameter_without_colon())
        ):
            return parameter_without_colon;
        self._reset(mark)
        return None;

    @memoize
    def defparameter(self) -> Optional[Any]:
        # defparameter: parameter [assign_token expression]
        mark = self._mark()
        if (
            (arg := self.parameter())
            and
            (default := self._tmp_56(),)
        ):
            return [arg , default [1] if default else None];
        self._reset(mark)
        return None;

    @memoize
    def parameter_starstar(self) -> Optional[Any]:
        # parameter_starstar: starstar_token parameter comma_token?
        mark = self._mark()
        if (
            (self.starstar_token())
            and
            (kwarg := self.parameter())
            and
            (self.comma_token(),)
        ):
            return dict ( kwarg = kwarg );
        self._reset(mark)
        return None;

    @memoize
    def parameter_list_starargs(self) -> Optional[Any]:
        # parameter_list_starargs: parameter_starstar | star_token parameter? ((comma_token defparameter))* [comma_token parameter_starstar?]
        mark = self._mark()
        if (
            (others := self.parameter_starstar())
        ):
            return dict ( vararg = None , kwonlyargs = [] , kw_defaults = [] ) | others;
        self._reset(mark)
        if (
            (self.star_token())
            and
            (vararg := self.parameter(),)
            and
            (kwonlyargs := self._loop0_57(),)
            and
            (others := self._tmp_58(),)
        ):
            return ( others [1] if others and others [1] else dict ( posonlyargs = [] , args = [] , kwonlyargs = [] , kw_defaults = [] , defaults = [] ) ) | dict ( vararg = vararg , kwonlyargs = [q [1] [0] for q in kwonlyargs] if kwonlyargs else [] , kw_defaults = [q [1] [1] for q in kwonlyargs] if kwonlyargs else [] , );
        self._reset(mark)
        return None;

    @memoize
    def parameter_list_no_posonly(self) -> Optional[Any]:
        # parameter_list_no_posonly: comma_token.defparameter+ [comma_token parameter_list_starargs?] | parameter_list_starargs
        mark = self._mark()
        if (
            (args := self._gather_59())
            and
            (others := self._tmp_61(),)
        ):
            return ( others [1] if others and others [1] else dict ( posonlyargs = [] , args = [] , kwonlyargs = [] , kw_defaults = [] , defaults = [] ) ) | dict ( args = [arg [0] for arg in args] , defaults = [arg [1] for arg in args] , );
        self._reset(mark)
        if (
            (others := self.parameter_list_starargs())
        ):
            return dict ( args = [] , defaults = [] ) | others;
        self._reset(mark)
        return None;

    @memoize
    def parameter_list(self) -> Optional[Any]:
        # parameter_list: comma_token.defparameter+ comma_token slash_token [comma_token parameter_list_no_posonly?] | parameter_list_no_posonly
        mark = self._mark()
        if (
            (posonlyargs := self._gather_62())
            and
            (self.comma_token())
            and
            (self.slash_token())
            and
            (others := self._tmp_64(),)
        ):
            return ( others [1] if others and others [1] else dict ( posonlyargs = [] , args = [] , kwonlyargs = [] , kw_defaults = [] , defaults = [] ) ) | dict ( posonlyargs = [arg [0] for arg in posonlyargs] , defaults = [arg [1] for arg in posonlyargs] + ( others [1] ['defaults'] if others [1] else [] ) , );
        self._reset(mark)
        if (
            (others := self.parameter_list_no_posonly())
        ):
            return dict ( posonlyargs = [] ) | others;
        self._reset(mark)
        return None;

    @memoize
    def parameter_list_preprocessed(self) -> Optional[Any]:
        # parameter_list_preprocessed: parameter_list
        mark = self._mark()
        if (
            (args := self.parameter_list())
        ):
            return args | dict ( defaults = [arg for arg in args ['defaults'] if arg] ) if not any ( [w is None and e is not None for q , w in enumerate ( args ['defaults'] ) for e in args ['defaults'] [: q]] ) else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def start_func_args(self) -> Optional[Any]:
        # start_func_args: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _args_level . append ( 1 )];
        self._reset(mark)
        return None;

    @memoize
    def stop_func_args(self) -> Optional[Any]:
        # stop_func_args: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _args_level . pop ( )];
        self._reset(mark)
        return None;

    @memoize
    def start_lmbd_args(self) -> Optional[Any]:
        # start_lmbd_args: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _args_level . append ( 0 )];
        self._reset(mark)
        return None;

    @memoize
    def stop_lmbd_args(self) -> Optional[Any]:
        # stop_lmbd_args: r''
        mark = self._mark()
        if (
            (self.expect(r''))
        ):
            return [self . _args_level . pop ( )];
        self._reset(mark)
        return None;

    @memoize
    def parameter_list_func(self) -> Optional[Any]:
        # parameter_list_func: start_func_args parameter_list_preprocessed? stop_func_args
        mark = self._mark()
        if (
            (token := self.start_func_args())
            and
            (args := self.parameter_list_preprocessed(),)
            and
            (self.stop_func_args())
        ):
            return ast . arguments ( token = token , ** args ) if args else ast . arguments ( token = token , posonlyargs = [] , args = [] , kwonlyargs = [] , kw_defaults = [] , defaults = [] );
        self._reset(mark)
        return None;

    @memoize
    def parameter_list_lmbd(self) -> Optional[Any]:
        # parameter_list_lmbd: start_lmbd_args parameter_list_preprocessed? stop_lmbd_args
        mark = self._mark()
        if (
            (token := self.start_lmbd_args())
            and
            (args := self.parameter_list_preprocessed(),)
            and
            (self.stop_lmbd_args())
        ):
            return ast . arguments ( token = token , ** args ) if args else ast . arguments ( token = token , posonlyargs = [] , args = [] , kwonlyargs = [] , kw_defaults = [] , defaults = [] );
        self._reset(mark)
        return None;

    @memoize
    def decorator(self) -> Optional[Any]:
        # decorator: indent decorator_token walrus_expression new_line
        mark = self._mark()
        if (
            (self.indent())
            and
            (self.decorator_token())
            and
            (a := self.walrus_expression())
            and
            (self.new_line())
        ):
            return a;
        self._reset(mark)
        return None;

    @memoize
    def func_def(self) -> Optional[Any]:
        # func_def: decorator* indent def_token identifier type_params? left_paren_token parameter_list_func right_paren_token [returns_token expression] colon_and_func_body
        mark = self._mark()
        if (
            (decorator_list := self._loop0_65(),)
            and
            (self.indent())
            and
            (token := self.def_token())
            and
            (name := self.identifier())
            and
            (type_params := self.type_params(),)
            and
            (self.left_paren_token())
            and
            (args := self.parameter_list_func())
            and
            (self.right_paren_token())
            and
            (returns := self._tmp_66(),)
            and
            (body := self.colon_and_func_body())
        ):
            return ast . FunctionDef ( token = token , decorator_list = decorator_list , name = + name , type_params = + type_params if type_params else [] , args = args , returns = returns [1] if returns else None , body = + body , );
        self._reset(mark)
        return None;

    @memoize
    def coro_def(self) -> Optional[Any]:
        # coro_def: decorator* indent async_token def_token identifier type_params? left_paren_token parameter_list_preprocessed? right_paren_token [returns_token expression] colon_and_coro_body
        mark = self._mark()
        if (
            (decorator_list := self._loop0_67(),)
            and
            (self.indent())
            and
            (token := self.async_token())
            and
            (self.def_token())
            and
            (name := self.identifier())
            and
            (type_params := self.type_params(),)
            and
            (self.left_paren_token())
            and
            (args := self.parameter_list_preprocessed(),)
            and
            (self.right_paren_token())
            and
            (returns := self._tmp_68(),)
            and
            (body := self.colon_and_coro_body())
        ):
            return ast . AsyncFunctionDef ( token = token , decorator_list = decorator_list , name = + name , type_params = + type_params if type_params else [] , args = ast . arguments ( token = name , ** args ) if args else ast . arguments ( token = name , posonlyargs = [] , args = [] , kwonlyargs = [] , kw_defaults = [] , defaults = [] ) , returns = returns [1] if returns else None , body = + body , );
        self._reset(mark)
        return None;

    @memoize
    def set_single_target(self) -> Optional[Any]:
        # set_single_target: left_paren_token set_single_target right_paren_token | set_attributeref | set_subscription | set_name
        mark = self._mark()
        if (
            (self.left_paren_token())
            and
            (target := self.set_single_target())
            and
            (self.right_paren_token())
        ):
            return [target [0] , 0];
        self._reset(mark)
        if (
            (target := self.set_attributeref())
        ):
            return [target , 0];
        self._reset(mark)
        if (
            (target := self.set_subscription())
        ):
            return [target , 0];
        self._reset(mark)
        if (
            (target := self.set_name())
        ):
            return [target , 1];
        self._reset(mark)
        return None;

    @memoize
    def del_single_target(self) -> Optional[Any]:
        # del_single_target: left_paren_token del_single_target right_paren_token | del_attributeref | del_subscription | del_name
        mark = self._mark()
        if (
            (self.left_paren_token())
            and
            (target := self.del_single_target())
            and
            (self.right_paren_token())
        ):
            return [target [0] , 0];
        self._reset(mark)
        if (
            (target := self.del_attributeref())
        ):
            return [target , 0];
        self._reset(mark)
        if (
            (target := self.del_subscription())
        ):
            return [target , 0];
        self._reset(mark)
        if (
            (target := self.del_name())
        ):
            return [target , 1];
        self._reset(mark)
        return None;

    @memoize
    def set_array_target(self) -> Optional[Any]:
        # set_array_target: set_single_target | left_paren_token set_multiple_targets? right_paren_token | left_bracket_token set_array_targets? right_bracket_token | star_token set_array_target
        mark = self._mark()
        if (
            (target := self.set_single_target())
        ):
            return target [0];
        self._reset(mark)
        if (
            (token := self.left_paren_token())
            and
            (targets := self.set_multiple_targets(),)
            and
            (self.right_paren_token())
        ):
            return targets if targets else ast . Tuple ( token = token , elts = [] , ctx = ast . Store ( token = token ) );
        self._reset(mark)
        if (
            (token := self.left_bracket_token())
            and
            (targets := self.set_array_targets(),)
            and
            (self.right_bracket_token())
        ):
            return ast . List ( token = token , elts = targets [0] if targets else [] , ctx = ast . Store ( token = token ) );
        self._reset(mark)
        if (
            (token := self.star_token())
            and
            (value := self.set_array_target())
        ):
            return None if isinstance ( value , ast . Starred ) else ast . Starred ( token = token , value = value , ctx = ast . Store ( token = token ) );
        self._reset(mark)
        return None;

    @memoize
    def del_array_target(self) -> Optional[Any]:
        # del_array_target: del_single_target | left_paren_token del_multiple_targets? right_paren_token | left_bracket_token del_array_targets? right_bracket_token | star_token del_array_target
        mark = self._mark()
        if (
            (target := self.del_single_target())
        ):
            return target [0];
        self._reset(mark)
        if (
            (token := self.left_paren_token())
            and
            (targets := self.del_multiple_targets(),)
            and
            (self.right_paren_token())
        ):
            return targets if targets else ast . Tuple ( token = token , elts = [] , ctx = ast . Del ( token = token ) );
        self._reset(mark)
        if (
            (token := self.left_bracket_token())
            and
            (targets := self.del_array_targets(),)
            and
            (self.right_bracket_token())
        ):
            return ast . List ( token = token , elts = targets [0] if targets else [] , ctx = ast . Del ( token = token ) );
        self._reset(mark)
        if (
            (token := self.star_token())
            and
            (value := self.del_array_target())
        ):
            return None if isinstance ( value , ast . Starred ) else ast . Starred ( token = token , value = value , ctx = ast . Del ( token = token ) );
        self._reset(mark)
        return None;

    @memoize
    def set_array_targets(self) -> Optional[Any]:
        # set_array_targets: comma_token.set_array_target+ comma_token?
        mark = self._mark()
        if (
            (_gather_69 := self._gather_69())
            and
            (opt := self.comma_token(),)
        ):
            return [_gather_69, opt];
        self._reset(mark)
        return None;

    @memoize
    def del_array_targets(self) -> Optional[Any]:
        # del_array_targets: comma_token.del_array_target+ comma_token?
        mark = self._mark()
        if (
            (_gather_71 := self._gather_71())
            and
            (opt := self.comma_token(),)
        ):
            return [_gather_71, opt];
        self._reset(mark)
        return None;

    @memoize
    def set_multiple_targets(self) -> Optional[Any]:
        # set_multiple_targets: set_array_targets
        mark = self._mark()
        if (
            (targets := self.set_array_targets())
        ):
            return ast . Tuple ( token = targets [0] [0] . token , elts = targets [0] , ctx = ast . Store ( token = targets [0] [0] . token ) ) if len ( targets [0] ) != 1 or targets [1] else targets [0] [0];
        self._reset(mark)
        return None;

    @memoize
    def del_multiple_targets(self) -> Optional[Any]:
        # del_multiple_targets: del_array_targets
        mark = self._mark()
        if (
            (targets := self.del_array_targets())
        ):
            return ast . Tuple ( token = targets [0] [0] . token , elts = targets [0] , ctx = ast . Del ( token = targets [0] [0] . token ) ) if len ( targets [0] ) != 1 or targets [1] else targets [0] [0];
        self._reset(mark)
        return None;

    @memoize
    def simple_stmt(self) -> Optional[Any]:
        # simple_stmt: assert_stmt | pass_stmt | del_stmt | return_stmt | raise_stmt | break_stmt | continue_stmt | import_stmt | global_stmt | nonlocal_stmt | type_stmt | augmented_assignment_stmt | annotated_assignment_stmt | assignment_stmt | expression_stmt
        mark = self._mark()
        if (
            (assert_stmt := self.assert_stmt())
        ):
            return assert_stmt;
        self._reset(mark)
        if (
            (pass_stmt := self.pass_stmt())
        ):
            return pass_stmt;
        self._reset(mark)
        if (
            (del_stmt := self.del_stmt())
        ):
            return del_stmt;
        self._reset(mark)
        if (
            (return_stmt := self.return_stmt())
        ):
            return return_stmt;
        self._reset(mark)
        if (
            (raise_stmt := self.raise_stmt())
        ):
            return raise_stmt;
        self._reset(mark)
        if (
            (break_stmt := self.break_stmt())
        ):
            return break_stmt;
        self._reset(mark)
        if (
            (continue_stmt := self.continue_stmt())
        ):
            return continue_stmt;
        self._reset(mark)
        if (
            (import_stmt := self.import_stmt())
        ):
            return import_stmt;
        self._reset(mark)
        if (
            (global_stmt := self.global_stmt())
        ):
            return global_stmt;
        self._reset(mark)
        if (
            (nonlocal_stmt := self.nonlocal_stmt())
        ):
            return nonlocal_stmt;
        self._reset(mark)
        if (
            (type_stmt := self.type_stmt())
        ):
            return type_stmt;
        self._reset(mark)
        if (
            (augmented_assignment_stmt := self.augmented_assignment_stmt())
        ):
            return augmented_assignment_stmt;
        self._reset(mark)
        if (
            (annotated_assignment_stmt := self.annotated_assignment_stmt())
        ):
            return annotated_assignment_stmt;
        self._reset(mark)
        if (
            (assignment_stmt := self.assignment_stmt())
        ):
            return assignment_stmt;
        self._reset(mark)
        if (
            (expression_stmt := self.expression_stmt())
        ):
            return expression_stmt;
        self._reset(mark)
        return None;

    @memoize
    def expression_stmt(self) -> Optional[Any]:
        # expression_stmt: yield_expression
        mark = self._mark()
        if (
            (a := self.yield_expression())
        ):
            return ast . Expr ( token = a , value = a );
        self._reset(mark)
        return None;

    @memoize
    def assignment_stmt(self) -> Optional[Any]:
        # assignment_stmt: ((set_multiple_targets assign_token))+ yield_expression
        mark = self._mark()
        if (
            (targets := self._loop1_73())
            and
            (value := self.yield_expression())
        ):
            return ast . Assign ( token = value . token , targets = [target [0] for target in targets] , value = value );
        self._reset(mark)
        return None;

    @memoize
    def augmented_assignment_stmt(self) -> Optional[Any]:
        # augmented_assignment_stmt: set_single_target aug_token yield_expression
        mark = self._mark()
        if (
            (target := self.set_single_target())
            and
            (op := self.aug_token())
            and
            (value := self.yield_expression())
        ):
            return ast . AugAssign ( token = value . token , target = target [0] , op = self . _bin_op_to_ast [op ( ) [: - 1]] , value = value );
        self._reset(mark)
        return None;

    @memoize
    def annotated_assignment_stmt(self) -> Optional[Any]:
        # annotated_assignment_stmt: set_single_target colon_token expression [assign_token yield_expression]
        mark = self._mark()
        if (
            (target := self.set_single_target())
            and
            (self.colon_token())
            and
            (annotation := self.expression())
            and
            (value := self._tmp_74(),)
        ):
            return ast . AnnAssign ( token = value . token , target = target [0] , annotation = annotation , value = value [1] if value else None , simple = target [1] );
        self._reset(mark)
        return None;

    @memoize
    def assert_stmt(self) -> Optional[Any]:
        # assert_stmt: assert_token expression
        mark = self._mark()
        if (
            (token := self.assert_token())
            and
            (test := self.expression())
        ):
            return ast . Assert ( token = token , test = test );
        self._reset(mark)
        return None;

    @memoize
    def pass_stmt(self) -> Optional[Any]:
        # pass_stmt: pass_token
        mark = self._mark()
        if (
            (token := self.pass_token())
        ):
            return ast . Pass ( token = token );
        self._reset(mark)
        return None;

    @memoize
    def del_stmt(self) -> Optional[Any]:
        # del_stmt: del_token del_array_targets
        mark = self._mark()
        if (
            (token := self.del_token())
            and
            (targets := self.del_array_targets())
        ):
            return ast . Delete ( token = token , targets = targets [0] );
        self._reset(mark)
        return None;

    @memoize
    def return_stmt(self) -> Optional[Any]:
        # return_stmt: return_token tupled_expression?
        mark = self._mark()
        if (
            (token := self.return_token())
            and
            (value := self.tupled_expression(),)
        ):
            return ast . Return ( token = token , value = value );
        self._reset(mark)
        return None;

    @memoize
    def raise_stmt(self) -> Optional[Any]:
        # raise_stmt: raise_token [expression [from_token expression]]
        mark = self._mark()
        if (
            (token := self.raise_token())
            and
            (exc := self._tmp_75(),)
        ):
            return ast . Raise ( token = token , exc = exc [0] if exc else None , cause = exc [1] [1] if exc and exc [1] else None );
        self._reset(mark)
        return None;

    @memoize
    def break_stmt(self) -> Optional[Any]:
        # break_stmt: break_token
        mark = self._mark()
        if (
            (token := self.break_token())
        ):
            return ast . Break ( token = token );
        self._reset(mark)
        return None;

    @memoize
    def continue_stmt(self) -> Optional[Any]:
        # continue_stmt: continue_token
        mark = self._mark()
        if (
            (token := self.continue_token())
        ):
            return ast . Continue ( token = token );
        self._reset(mark)
        return None;

    @memoize
    def module(self) -> Optional[Any]:
        # module: dot_token+ &import_token !import_token? | dot_token* dot_token.identifier+
        mark = self._mark()
        if (
            (_loop1_76 := self._loop1_76())
            and
            (self.positive_lookahead(self.import_token, ))
            and
            (opt := self.negative_lookahead(self.import_token, ),)
        ):
            return [_loop1_76, opt];
        self._reset(mark)
        if (
            (_loop0_77 := self._loop0_77(),)
            and
            (_gather_78 := self._gather_78())
        ):
            return [_loop0_77, _gather_78];
        self._reset(mark)
        return None;

    @memoize
    def import_stmt(self) -> Optional[Any]:
        # import_stmt: import_token comma_token.(dot_token.identifier+ [as_token identifier])+ | from_token module import_token comma_token.(identifier [as_token identifier])+ | from_token module import_token left_paren_token comma_token.(identifier [as_token identifier])+ comma_token? right_paren_token | from_token module import_token star_token
        mark = self._mark()
        if (
            (token := self.import_token())
            and
            (names := self._gather_80())
        ):
            return ast . Import ( token = token , names = [ast . alias ( token = token , name = '.' . join ( [+ c for c in name [0]] ) , asname = + name [1] [1] if name [1] else None ) for name in names] );
        self._reset(mark)
        if (
            (token := self.from_token())
            and
            (module := self.module())
            and
            (self.import_token())
            and
            (names := self._gather_82())
        ):
            return ast . ImportFrom ( token = token , module = '.' . join ( [+ c for c in module [1]] ) if module [1] else None , names = [ast . alias ( token = token , name = + name [0] , asname = + name [1] [1] if name [1] else None ) for name in names] , level = len ( module [0] ) );
        self._reset(mark)
        if (
            (token := self.from_token())
            and
            (module := self.module())
            and
            (self.import_token())
            and
            (self.left_paren_token())
            and
            (names := self._gather_84())
            and
            (self.comma_token(),)
            and
            (self.right_paren_token())
        ):
            return ast . ImportFrom ( token = token , module = '.' . join ( [+ c for c in module [1]] ) if module [1] else None , names = [ast . alias ( token = token , name = + name [0] , asname = + name [1] [1] if name [1] else None ) for name in names] , level = len ( module [0] ) );
        self._reset(mark)
        if (
            (token := self.from_token())
            and
            (module := self.module())
            and
            (self.import_token())
            and
            (self.star_token())
        ):
            return ast . ImportFrom ( token = token , module = '.' . join ( [+ c for c in module [1]] ) if module [1] else None , names = [ast . alias ( token , name = '*' )] , level = len ( module [0] ) ) if self . _func_level [- 1] == 0 else self . _error ( );
        self._reset(mark)
        return None;

    @memoize
    def global_stmt(self) -> Optional[Any]:
        # global_stmt: global_token comma_token.identifier+
        mark = self._mark()
        if (
            (token := self.global_token())
            and
            (names := self._gather_86())
        ):
            return ast . Global ( token = token , names = [+ name for name in names] );
        self._reset(mark)
        return None;

    @memoize
    def nonlocal_stmt(self) -> Optional[Any]:
        # nonlocal_stmt: nonlocal_token comma_token.identifier+
        mark = self._mark()
        if (
            (token := self.nonlocal_token())
            and
            (names := self._gather_88())
        ):
            return ast . Nonlocal ( token = token , names = [+ name for name in names] );
        self._reset(mark)
        return None;

    @memoize
    def type_stmt(self) -> Optional[Any]:
        # type_stmt: type_token set_name type_params? assign_token expression
        mark = self._mark()
        if (
            (token := self.type_token())
            and
            (name := self.set_name())
            and
            (type_params := self.type_params(),)
            and
            (self.assign_token())
            and
            (value := self.expression())
        ):
            return ast . TypeAlias ( token = token , name = name , type_params = + type_params if type_params else [] , value = value , );
        self._reset(mark)
        return None;

    @memoize
    def type_params(self) -> Optional[Any]:
        # type_params: left_bracket_token comma_token.type_param+ right_bracket_token
        mark = self._mark()
        if (
            (self.left_bracket_token())
            and
            (params := self._gather_90())
            and
            (self.right_bracket_token())
        ):
            return self . _make_true ( params );
        self._reset(mark)
        return None;

    @memoize
    def type_param(self) -> Optional[Any]:
        # type_param: identifier colon_token expression | identifier | star_token identifier | starstar_token identifier
        mark = self._mark()
        if (
            (name := self.identifier())
            and
            (self.colon_token())
            and
            (bound := self.expression())
        ):
            return ast . TypeVar ( token = name , name = + name , bound = bound );
        self._reset(mark)
        if (
            (name := self.identifier())
        ):
            return ast . TypeVar ( token = name , name = + name );
        self._reset(mark)
        if (
            (self.star_token())
            and
            (name := self.identifier())
        ):
            return ast . TypeVarTuple ( token = name , name = + name );
        self._reset(mark)
        if (
            (self.starstar_token())
            and
            (name := self.identifier())
        ):
            return ast . ParamSpec ( token = name , name = + name );
        self._reset(mark)
        return None;

    @memoize
    def _tmp_1(self) -> Optional[Any]:
        # _tmp_1: r':' start_stre_b formatted_str_end | r'\}'
        mark = self._mark()
        if (
            (literal := self.expect(r':'))
            and
            (start_stre_b := self.start_stre_b())
            and
            (formatted_str_end := self.formatted_str_end())
        ):
            return [literal, start_stre_b, formatted_str_end];
        self._reset(mark)
        if (
            (literal := self.expect(r'\}'))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_2(self) -> Optional[Any]:
        # _tmp_2: r':' start_stre_b formatted_str_end | r'\}'
        mark = self._mark()
        if (
            (literal := self.expect(r':'))
            and
            (start_stre_b := self.start_stre_b())
            and
            (formatted_str_end := self.formatted_str_end())
        ):
            return [literal, start_stre_b, formatted_str_end];
        self._reset(mark)
        if (
            (literal := self.expect(r'\}'))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_3(self) -> Optional[Any]:
        # _tmp_3: digitpart | pointfloat
        mark = self._mark()
        if (
            (digitpart := self.digitpart())
        ):
            return digitpart;
        self._reset(mark)
        if (
            (pointfloat := self.pointfloat())
        ):
            return pointfloat;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_4(self) -> Optional[Any]:
        # _tmp_4: floatnumber | digitpart
        mark = self._mark()
        if (
            (floatnumber := self.floatnumber())
        ):
            return floatnumber;
        self._reset(mark)
        if (
            (digitpart := self.digitpart())
        ):
            return digitpart;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_5(self) -> Optional[Any]:
        # _tmp_5: colon_token expression?
        mark = self._mark()
        if (
            (colon_token := self.colon_token())
            and
            (opt := self.expression(),)
        ):
            return [colon_token, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_7(self) -> Optional[Any]:
        # _loop0_7: comma_token sub_item
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.sub_item())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_6(self) -> Optional[Any]:
        # _gather_6: sub_item _loop0_7
        mark = self._mark()
        if (
            (elem := self.sub_item())
            is not None
            and
            (seq := self._loop0_7())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop1_8(self) -> Optional[Any]:
        # _loop1_8: (async_token? for_token set_multiple_targets in_token ready_to_if_expr ((if_token ready_to_if_expr))*)
        mark = self._mark()
        children = []
        while (
            (_tmp_92 := self._tmp_92())
        ):
            children.append(_tmp_92)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_10(self) -> Optional[Any]:
        # _loop0_10: comma_token display_item
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.display_item())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_9(self) -> Optional[Any]:
        # _gather_9: display_item _loop0_10
        mark = self._mark()
        if (
            (elem := self.display_item())
            is not None
            and
            (seq := self._loop0_10())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_12(self) -> Optional[Any]:
        # _loop0_12: comma_token dict_display_item
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.dict_display_item())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_11(self) -> Optional[Any]:
        # _gather_11: dict_display_item _loop0_12
        mark = self._mark()
        if (
            (elem := self.dict_display_item())
            is not None
            and
            (seq := self._loop0_12())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_13(self) -> Optional[Any]:
        # _tmp_13: comma_token.call_arg+ comma_token?
        mark = self._mark()
        if (
            (_gather_93 := self._gather_93())
            and
            (opt := self.comma_token(),)
        ):
            return [_gather_93, opt];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_14(self) -> Optional[Any]:
        # _tmp_14: walrus_expression comp_for | call_args
        mark = self._mark()
        if (
            (walrus_expression := self.walrus_expression())
            and
            (comp_for := self.comp_for())
        ):
            return [walrus_expression, comp_for];
        self._reset(mark)
        if (
            (call_args := self.call_args())
        ):
            return call_args;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_15(self) -> Optional[Any]:
        # _tmp_15: starstar_token u_expr
        mark = self._mark()
        if (
            (starstar_token := self.starstar_token())
            and
            (u_expr := self.u_expr())
        ):
            return [starstar_token, u_expr];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_16(self) -> Optional[Any]:
        # _loop0_16: (m_token u_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_95 := self._tmp_95())
        ):
            children.append(_tmp_95)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_17(self) -> Optional[Any]:
        # _loop0_17: (a_token m_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_96 := self._tmp_96())
        ):
            children.append(_tmp_96)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_18(self) -> Optional[Any]:
        # _loop0_18: (shift_token a_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_97 := self._tmp_97())
        ):
            children.append(_tmp_97)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_19(self) -> Optional[Any]:
        # _loop0_19: (bit_and_token shift_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_98 := self._tmp_98())
        ):
            children.append(_tmp_98)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_20(self) -> Optional[Any]:
        # _loop0_20: (bit_xor_token and_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_99 := self._tmp_99())
        ):
            children.append(_tmp_99)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_21(self) -> Optional[Any]:
        # _loop0_21: (bit_or_token xor_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_100 := self._tmp_100())
        ):
            children.append(_tmp_100)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_22(self) -> Optional[Any]:
        # _loop0_22: (comp_operator ready_to_be_starred_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_101 := self._tmp_101())
        ):
            children.append(_tmp_101)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_23(self) -> Optional[Any]:
        # _loop0_23: (and_token not_test)
        mark = self._mark()
        children = []
        while (
            (_tmp_102 := self._tmp_102())
        ):
            children.append(_tmp_102)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_24(self) -> Optional[Any]:
        # _loop0_24: (or_token and_test)
        mark = self._mark()
        children = []
        while (
            (_tmp_103 := self._tmp_103())
        ):
            children.append(_tmp_103)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_25(self) -> Optional[Any]:
        # _tmp_25: if_token ready_to_if_expr else_token expression
        mark = self._mark()
        if (
            (if_token := self.if_token())
            and
            (ready_to_if_expr := self.ready_to_if_expr())
            and
            (else_token := self.else_token())
            and
            (expression := self.expression())
        ):
            return [if_token, ready_to_if_expr, else_token, expression];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_27(self) -> Optional[Any]:
        # _loop0_27: comma_token expression
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.expression())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_26(self) -> Optional[Any]:
        # _gather_26: expression _loop0_27
        mark = self._mark()
        if (
            (elem := self.expression())
            is not None
            and
            (seq := self._loop0_27())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_28(self) -> Optional[Any]:
        # _tmp_28: set_name walrus_token
        mark = self._mark()
        if (
            (set_name := self.set_name())
            and
            (walrus_token := self.walrus_token())
        ):
            return [set_name, walrus_token];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_30(self) -> Optional[Any]:
        # _loop0_30: (semicolon_token) simple_stmt
        mark = self._mark()
        children = []
        while (
            (self.semicolon_token())
            and
            (elem := self.simple_stmt())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_29(self) -> Optional[Any]:
        # _gather_29: simple_stmt _loop0_30
        mark = self._mark()
        if (
            (elem := self.simple_stmt())
            is not None
            and
            (seq := self._loop0_30())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop1_31(self) -> Optional[Any]:
        # _loop1_31: line
        mark = self._mark()
        children = []
        while (
            (line := self.line())
        ):
            children.append(line)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_32(self) -> Optional[Any]:
        # _loop0_32: line
        mark = self._mark()
        children = []
        while (
            (line := self.line())
        ):
            children.append(line)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_33(self) -> Optional[Any]:
        # _loop0_33: (indent elif_token walrus_expression colon_and_body)
        mark = self._mark()
        children = []
        while (
            (_tmp_104 := self._tmp_104())
        ):
            children.append(_tmp_104)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_34(self) -> Optional[Any]:
        # _tmp_34: indent else_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (else_token := self.else_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, else_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_35(self) -> Optional[Any]:
        # _tmp_35: indent else_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (else_token := self.else_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, else_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_36(self) -> Optional[Any]:
        # _tmp_36: indent else_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (else_token := self.else_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, else_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_37(self) -> Optional[Any]:
        # _tmp_37: indent else_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (else_token := self.else_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, else_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_38(self) -> Optional[Any]:
        # _tmp_38: try_group | try_except | try_finally
        mark = self._mark()
        if (
            (try_group := self.try_group())
        ):
            return try_group;
        self._reset(mark)
        if (
            (try_except := self.try_except())
        ):
            return try_except;
        self._reset(mark)
        if (
            (try_finally := self.try_finally())
        ):
            return try_finally;
        self._reset(mark)
        return None;

    @memoize
    def _loop1_39(self) -> Optional[Any]:
        # _loop1_39: except_colon
        mark = self._mark()
        children = []
        while (
            (except_colon := self.except_colon())
        ):
            children.append(except_colon)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_40(self) -> Optional[Any]:
        # _tmp_40: indent else_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (else_token := self.else_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, else_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_41(self) -> Optional[Any]:
        # _tmp_41: indent finally_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (finally_token := self.finally_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, finally_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _loop1_42(self) -> Optional[Any]:
        # _loop1_42: except_star
        mark = self._mark()
        children = []
        while (
            (except_star := self.except_star())
        ):
            children.append(except_star)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_43(self) -> Optional[Any]:
        # _tmp_43: indent else_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (else_token := self.else_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, else_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_44(self) -> Optional[Any]:
        # _tmp_44: indent finally_token colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (finally_token := self.finally_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, finally_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_45(self) -> Optional[Any]:
        # _loop0_45: (!'')
        mark = self._mark()
        children = []
        while (
            (self.negative_lookahead(self.expect, ''))
        ):
            children.append([])
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_46(self) -> Optional[Any]:
        # _loop0_46: (!'')
        mark = self._mark()
        children = []
        while (
            (self.negative_lookahead(self.expect, ''))
        ):
            children.append([])
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_47(self) -> Optional[Any]:
        # _tmp_47: finally_token colon_and_body
        mark = self._mark()
        if (
            (finally_token := self.finally_token())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [finally_token, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_48(self) -> Optional[Any]:
        # _tmp_48: expression [as_token identifier]
        mark = self._mark()
        if (
            (expression := self.expression())
            and
            (opt := self._tmp_105(),)
        ):
            return [expression, opt];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_49(self) -> Optional[Any]:
        # _tmp_49: expression [as_token identifier]
        mark = self._mark()
        if (
            (expression := self.expression())
            and
            (opt := self._tmp_106(),)
        ):
            return [expression, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_51(self) -> Optional[Any]:
        # _loop0_51: comma_token (expression [as_token set_single_target])
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self._tmp_107())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_50(self) -> Optional[Any]:
        # _gather_50: (expression [as_token set_single_target]) _loop0_51
        mark = self._mark()
        if (
            (elem := self._tmp_107())
            is not None
            and
            (seq := self._loop0_51())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_53(self) -> Optional[Any]:
        # _loop0_53: comma_token (expression [as_token set_single_target])
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self._tmp_108())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_52(self) -> Optional[Any]:
        # _gather_52: (expression [as_token set_single_target]) _loop0_53
        mark = self._mark()
        if (
            (elem := self._tmp_108())
            is not None
            and
            (seq := self._loop0_53())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_54(self) -> Optional[Any]:
        # _loop0_54: decorator
        mark = self._mark()
        children = []
        while (
            (decorator := self.decorator())
        ):
            children.append(decorator)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_55(self) -> Optional[Any]:
        # _tmp_55: left_paren_token call_args right_paren_token
        mark = self._mark()
        if (
            (left_paren_token := self.left_paren_token())
            and
            (call_args := self.call_args())
            and
            (right_paren_token := self.right_paren_token())
        ):
            return [left_paren_token, call_args, right_paren_token];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_56(self) -> Optional[Any]:
        # _tmp_56: assign_token expression
        mark = self._mark()
        if (
            (assign_token := self.assign_token())
            and
            (expression := self.expression())
        ):
            return [assign_token, expression];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_57(self) -> Optional[Any]:
        # _loop0_57: (comma_token defparameter)
        mark = self._mark()
        children = []
        while (
            (_tmp_109 := self._tmp_109())
        ):
            children.append(_tmp_109)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_58(self) -> Optional[Any]:
        # _tmp_58: comma_token parameter_starstar?
        mark = self._mark()
        if (
            (comma_token := self.comma_token())
            and
            (opt := self.parameter_starstar(),)
        ):
            return [comma_token, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_60(self) -> Optional[Any]:
        # _loop0_60: comma_token defparameter
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.defparameter())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_59(self) -> Optional[Any]:
        # _gather_59: defparameter _loop0_60
        mark = self._mark()
        if (
            (elem := self.defparameter())
            is not None
            and
            (seq := self._loop0_60())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_61(self) -> Optional[Any]:
        # _tmp_61: comma_token parameter_list_starargs?
        mark = self._mark()
        if (
            (comma_token := self.comma_token())
            and
            (opt := self.parameter_list_starargs(),)
        ):
            return [comma_token, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_63(self) -> Optional[Any]:
        # _loop0_63: comma_token defparameter
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.defparameter())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_62(self) -> Optional[Any]:
        # _gather_62: defparameter _loop0_63
        mark = self._mark()
        if (
            (elem := self.defparameter())
            is not None
            and
            (seq := self._loop0_63())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_64(self) -> Optional[Any]:
        # _tmp_64: comma_token parameter_list_no_posonly?
        mark = self._mark()
        if (
            (comma_token := self.comma_token())
            and
            (opt := self.parameter_list_no_posonly(),)
        ):
            return [comma_token, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_65(self) -> Optional[Any]:
        # _loop0_65: decorator
        mark = self._mark()
        children = []
        while (
            (decorator := self.decorator())
        ):
            children.append(decorator)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_66(self) -> Optional[Any]:
        # _tmp_66: returns_token expression
        mark = self._mark()
        if (
            (returns_token := self.returns_token())
            and
            (expression := self.expression())
        ):
            return [returns_token, expression];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_67(self) -> Optional[Any]:
        # _loop0_67: decorator
        mark = self._mark()
        children = []
        while (
            (decorator := self.decorator())
        ):
            children.append(decorator)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_68(self) -> Optional[Any]:
        # _tmp_68: returns_token expression
        mark = self._mark()
        if (
            (returns_token := self.returns_token())
            and
            (expression := self.expression())
        ):
            return [returns_token, expression];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_70(self) -> Optional[Any]:
        # _loop0_70: comma_token set_array_target
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.set_array_target())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_69(self) -> Optional[Any]:
        # _gather_69: set_array_target _loop0_70
        mark = self._mark()
        if (
            (elem := self.set_array_target())
            is not None
            and
            (seq := self._loop0_70())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_72(self) -> Optional[Any]:
        # _loop0_72: comma_token del_array_target
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.del_array_target())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_71(self) -> Optional[Any]:
        # _gather_71: del_array_target _loop0_72
        mark = self._mark()
        if (
            (elem := self.del_array_target())
            is not None
            and
            (seq := self._loop0_72())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop1_73(self) -> Optional[Any]:
        # _loop1_73: (set_multiple_targets assign_token)
        mark = self._mark()
        children = []
        while (
            (_tmp_110 := self._tmp_110())
        ):
            children.append(_tmp_110)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_74(self) -> Optional[Any]:
        # _tmp_74: assign_token yield_expression
        mark = self._mark()
        if (
            (assign_token := self.assign_token())
            and
            (yield_expression := self.yield_expression())
        ):
            return [assign_token, yield_expression];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_75(self) -> Optional[Any]:
        # _tmp_75: expression [from_token expression]
        mark = self._mark()
        if (
            (expression := self.expression())
            and
            (opt := self._tmp_111(),)
        ):
            return [expression, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop1_76(self) -> Optional[Any]:
        # _loop1_76: dot_token
        mark = self._mark()
        children = []
        while (
            (dot_token := self.dot_token())
        ):
            children.append(dot_token)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_77(self) -> Optional[Any]:
        # _loop0_77: dot_token
        mark = self._mark()
        children = []
        while (
            (dot_token := self.dot_token())
        ):
            children.append(dot_token)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_79(self) -> Optional[Any]:
        # _loop0_79: dot_token identifier
        mark = self._mark()
        children = []
        while (
            (self.dot_token())
            and
            (elem := self.identifier())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_78(self) -> Optional[Any]:
        # _gather_78: identifier _loop0_79
        mark = self._mark()
        if (
            (elem := self.identifier())
            is not None
            and
            (seq := self._loop0_79())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_81(self) -> Optional[Any]:
        # _loop0_81: comma_token (dot_token.identifier+ [as_token identifier])
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self._tmp_112())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_80(self) -> Optional[Any]:
        # _gather_80: (dot_token.identifier+ [as_token identifier]) _loop0_81
        mark = self._mark()
        if (
            (elem := self._tmp_112())
            is not None
            and
            (seq := self._loop0_81())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_83(self) -> Optional[Any]:
        # _loop0_83: comma_token (identifier [as_token identifier])
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self._tmp_113())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_82(self) -> Optional[Any]:
        # _gather_82: (identifier [as_token identifier]) _loop0_83
        mark = self._mark()
        if (
            (elem := self._tmp_113())
            is not None
            and
            (seq := self._loop0_83())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_85(self) -> Optional[Any]:
        # _loop0_85: comma_token (identifier [as_token identifier])
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self._tmp_114())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_84(self) -> Optional[Any]:
        # _gather_84: (identifier [as_token identifier]) _loop0_85
        mark = self._mark()
        if (
            (elem := self._tmp_114())
            is not None
            and
            (seq := self._loop0_85())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_87(self) -> Optional[Any]:
        # _loop0_87: comma_token identifier
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.identifier())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_86(self) -> Optional[Any]:
        # _gather_86: identifier _loop0_87
        mark = self._mark()
        if (
            (elem := self.identifier())
            is not None
            and
            (seq := self._loop0_87())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_89(self) -> Optional[Any]:
        # _loop0_89: comma_token identifier
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.identifier())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_88(self) -> Optional[Any]:
        # _gather_88: identifier _loop0_89
        mark = self._mark()
        if (
            (elem := self.identifier())
            is not None
            and
            (seq := self._loop0_89())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_91(self) -> Optional[Any]:
        # _loop0_91: comma_token type_param
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.type_param())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_90(self) -> Optional[Any]:
        # _gather_90: type_param _loop0_91
        mark = self._mark()
        if (
            (elem := self.type_param())
            is not None
            and
            (seq := self._loop0_91())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_92(self) -> Optional[Any]:
        # _tmp_92: async_token? for_token set_multiple_targets in_token ready_to_if_expr ((if_token ready_to_if_expr))*
        mark = self._mark()
        if (
            (opt := self.async_token(),)
            and
            (for_token := self.for_token())
            and
            (set_multiple_targets := self.set_multiple_targets())
            and
            (in_token := self.in_token())
            and
            (ready_to_if_expr := self.ready_to_if_expr())
            and
            (_loop0_115 := self._loop0_115(),)
        ):
            return [opt, for_token, set_multiple_targets, in_token, ready_to_if_expr, _loop0_115];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_94(self) -> Optional[Any]:
        # _loop0_94: comma_token call_arg
        mark = self._mark()
        children = []
        while (
            (self.comma_token())
            and
            (elem := self.call_arg())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_93(self) -> Optional[Any]:
        # _gather_93: call_arg _loop0_94
        mark = self._mark()
        if (
            (elem := self.call_arg())
            is not None
            and
            (seq := self._loop0_94())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_95(self) -> Optional[Any]:
        # _tmp_95: m_token u_expr
        mark = self._mark()
        if (
            (m_token := self.m_token())
            and
            (u_expr := self.u_expr())
        ):
            return [m_token, u_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_96(self) -> Optional[Any]:
        # _tmp_96: a_token m_expr
        mark = self._mark()
        if (
            (a_token := self.a_token())
            and
            (m_expr := self.m_expr())
        ):
            return [a_token, m_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_97(self) -> Optional[Any]:
        # _tmp_97: shift_token a_expr
        mark = self._mark()
        if (
            (shift_token := self.shift_token())
            and
            (a_expr := self.a_expr())
        ):
            return [shift_token, a_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_98(self) -> Optional[Any]:
        # _tmp_98: bit_and_token shift_expr
        mark = self._mark()
        if (
            (bit_and_token := self.bit_and_token())
            and
            (shift_expr := self.shift_expr())
        ):
            return [bit_and_token, shift_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_99(self) -> Optional[Any]:
        # _tmp_99: bit_xor_token and_expr
        mark = self._mark()
        if (
            (bit_xor_token := self.bit_xor_token())
            and
            (and_expr := self.and_expr())
        ):
            return [bit_xor_token, and_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_100(self) -> Optional[Any]:
        # _tmp_100: bit_or_token xor_expr
        mark = self._mark()
        if (
            (bit_or_token := self.bit_or_token())
            and
            (xor_expr := self.xor_expr())
        ):
            return [bit_or_token, xor_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_101(self) -> Optional[Any]:
        # _tmp_101: comp_operator ready_to_be_starred_expr
        mark = self._mark()
        if (
            (comp_operator := self.comp_operator())
            and
            (ready_to_be_starred_expr := self.ready_to_be_starred_expr())
        ):
            return [comp_operator, ready_to_be_starred_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_102(self) -> Optional[Any]:
        # _tmp_102: and_token not_test
        mark = self._mark()
        if (
            (and_token := self.and_token())
            and
            (not_test := self.not_test())
        ):
            return [and_token, not_test];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_103(self) -> Optional[Any]:
        # _tmp_103: or_token and_test
        mark = self._mark()
        if (
            (or_token := self.or_token())
            and
            (and_test := self.and_test())
        ):
            return [or_token, and_test];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_104(self) -> Optional[Any]:
        # _tmp_104: indent elif_token walrus_expression colon_and_body
        mark = self._mark()
        if (
            (indent := self.indent())
            and
            (elif_token := self.elif_token())
            and
            (walrus_expression := self.walrus_expression())
            and
            (colon_and_body := self.colon_and_body())
        ):
            return [indent, elif_token, walrus_expression, colon_and_body];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_105(self) -> Optional[Any]:
        # _tmp_105: as_token identifier
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (identifier := self.identifier())
        ):
            return [as_token, identifier];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_106(self) -> Optional[Any]:
        # _tmp_106: as_token identifier
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (identifier := self.identifier())
        ):
            return [as_token, identifier];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_107(self) -> Optional[Any]:
        # _tmp_107: expression [as_token set_single_target]
        mark = self._mark()
        if (
            (expression := self.expression())
            and
            (opt := self._tmp_116(),)
        ):
            return [expression, opt];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_108(self) -> Optional[Any]:
        # _tmp_108: expression [as_token set_single_target]
        mark = self._mark()
        if (
            (expression := self.expression())
            and
            (opt := self._tmp_117(),)
        ):
            return [expression, opt];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_109(self) -> Optional[Any]:
        # _tmp_109: comma_token defparameter
        mark = self._mark()
        if (
            (comma_token := self.comma_token())
            and
            (defparameter := self.defparameter())
        ):
            return [comma_token, defparameter];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_110(self) -> Optional[Any]:
        # _tmp_110: set_multiple_targets assign_token
        mark = self._mark()
        if (
            (set_multiple_targets := self.set_multiple_targets())
            and
            (assign_token := self.assign_token())
        ):
            return [set_multiple_targets, assign_token];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_111(self) -> Optional[Any]:
        # _tmp_111: from_token expression
        mark = self._mark()
        if (
            (from_token := self.from_token())
            and
            (expression := self.expression())
        ):
            return [from_token, expression];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_112(self) -> Optional[Any]:
        # _tmp_112: dot_token.identifier+ [as_token identifier]
        mark = self._mark()
        if (
            (_gather_118 := self._gather_118())
            and
            (opt := self._tmp_120(),)
        ):
            return [_gather_118, opt];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_113(self) -> Optional[Any]:
        # _tmp_113: identifier [as_token identifier]
        mark = self._mark()
        if (
            (identifier := self.identifier())
            and
            (opt := self._tmp_121(),)
        ):
            return [identifier, opt];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_114(self) -> Optional[Any]:
        # _tmp_114: identifier [as_token identifier]
        mark = self._mark()
        if (
            (identifier := self.identifier())
            and
            (opt := self._tmp_122(),)
        ):
            return [identifier, opt];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_115(self) -> Optional[Any]:
        # _loop0_115: (if_token ready_to_if_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_123 := self._tmp_123())
        ):
            children.append(_tmp_123)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_116(self) -> Optional[Any]:
        # _tmp_116: as_token set_single_target
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (set_single_target := self.set_single_target())
        ):
            return [as_token, set_single_target];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_117(self) -> Optional[Any]:
        # _tmp_117: as_token set_single_target
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (set_single_target := self.set_single_target())
        ):
            return [as_token, set_single_target];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_119(self) -> Optional[Any]:
        # _loop0_119: dot_token identifier
        mark = self._mark()
        children = []
        while (
            (self.dot_token())
            and
            (elem := self.identifier())
        ):
            children.append(elem)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _gather_118(self) -> Optional[Any]:
        # _gather_118: identifier _loop0_119
        mark = self._mark()
        if (
            (elem := self.identifier())
            is not None
            and
            (seq := self._loop0_119())
            is not None
        ):
            return [elem] + seq;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_120(self) -> Optional[Any]:
        # _tmp_120: as_token identifier
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (identifier := self.identifier())
        ):
            return [as_token, identifier];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_121(self) -> Optional[Any]:
        # _tmp_121: as_token identifier
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (identifier := self.identifier())
        ):
            return [as_token, identifier];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_122(self) -> Optional[Any]:
        # _tmp_122: as_token identifier
        mark = self._mark()
        if (
            (as_token := self.as_token())
            and
            (identifier := self.identifier())
        ):
            return [as_token, identifier];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_123(self) -> Optional[Any]:
        # _tmp_123: if_token ready_to_if_expr
        mark = self._mark()
        if (
            (if_token := self.if_token())
            and
            (ready_to_if_expr := self.ready_to_if_expr())
        ):
            return [if_token, ready_to_if_expr];
        self._reset(mark)
        return None;

    KEYWORDS = ('False', 'None', 'True')
    SOFT_KEYWORDS = ()


if __name__ == '__main__':
    from pegen.parser import simple_parser_main
    simple_parser_main(GeneratedParser)
